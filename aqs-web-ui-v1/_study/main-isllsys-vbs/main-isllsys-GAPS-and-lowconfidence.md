# Main_ISLLSYS_20010101.vbs — reconstruction gaps & low-confidence spans (per band)

## Band 4434-4447  (first: window_onload, last: LogoutMain)
**Gaps:** IMG_4445 -> IMG_4446: gap confirmed by viewing the source photos. In Sub ClearActionMenus (Else branch), the For loop that iterates fraMain.children and blanks each type="action" frame's src is missing between 'Set objFrames = fraMain.children' (end of IMG_4445) and 'Set objFrames = Nothing' (top of IMG_4446). Marker inserted. IMG_4446 -> IMG_4447: gap confirmed by photos. IMG_4446 ends at 'End Sub' of AddWindowToCollection (bottom of frame) and IMG_4447 opens directly on 'Function LogoutMain()'; the connective separator/blank line(s) between the two routines were not photographed. Marker inserted. (Note: the IMG_4442 -> IMG_4443 seam has no duplicated overlap line but is NOT a gap -- verified against the photos it is one continuous If/ElseIf/Else/End If on CLng(mstrPolicyID) closed by a single End If, so no content was skipped.)

**Low-confidence:** No guillemet illegibility markers appear anywhere in this band. (The literal "???" in the mstrReturnValue comment from IMG_4435 is verbatim source text, not an illegibility marker, and is preserved as-is.) Minor uncertainty reconciled at the IMG_4441/IMG_4442 seam: the End If that closes the mdicNonPolicyChildWindows block reads at 4-space indent in IMG_4441 but 8-space at the top of IMG_4442; treated as a single overlapping line and rendered once at 4-space, followed by the End If that closes the 'If mblnReplacementHomePage' block.

## Band 4448-4461  (first: continuation, last: DeleteAction)
**Gaps:** IMG_4452->IMG_4453: zero duplicate-line overlap at seam; lines appear logically continuous (strWindowOptions defined at end of IMG_4452, then immediately consumed by window.showModelessDialog at start of IMG_4453) — marker inserted per protocol but a skip is unlikely. IMG_4455->IMG_4456: zero overlap; the ShowFrame PURPOSE comment block continues ('...download of the action menu' -> 'This sub sets the size of the selected frame...') — marker inserted per protocol, a blank comment line may have been skipped. IMG_4456->IMG_4457: zero overlap; If-true branch ('...allow tab access') flows directly into 'Else' of the same If — marker inserted per protocol but a skip is unlikely (note: the Else branch appears to lack a 'strFrameRowSizes = strFrameRowSizes & "0"' append line, but that would sit inside IMG_4457, not at the seam). IMG_4460->IMG_4461: DEFINITE gap — IMG_4460 ends at 'With mxmlBatchEdits' and IMG_4461 opens at 'End With', so the body of the 'With mxmlBatchEdits' block (the statements that load strMessage into mxmlBatchEdits) was skipped between the two photos and is genuinely missing.

**Low-confidence:** IMG_4455: both window.open statements in Sub ParentWindow_Navigate end with '...&diagnosticmode=" & mstrDiagnosticMode & "&redirec‹?›' — the querystring past '&redirec' runs off the right edge of the editor and is illegible/cut off; the guillemet-question-guillemet placeholder (‹?›) is preserved verbatim at both truncation points.

## Band 4462-4475  (first: continuation, last: ExecuteAction)
**Gaps:** Between IMG_4472 and IMG_4473: no shared overlapping line — the scroll skipped content between the end of the ExecuteAction querystring build ('"&debug=" & Cstr(mblnDeveloperMessages)') and the resumption at 'Call TrapBrowserError("ExecuteAction|1")'. Marked inline with a GAP marker.

**Low-confidence:** No illegible «?» placeholders appear anywhere in this band. Formatting-only uncertainty (not content): indentation DEPTH is transcribed inconsistently across photos because horizontal scale differs between shots — IMG_4462–4465 under-indent the DeleteAction Select Case block (Case labels ~8 sp) while IMG_4466 (verified against the source photo) captures the true deeper nesting (Case Else ~16 sp, End Select ~12 sp). At the IMG_4465→IMG_4466 seam I kept IMG_4466's version of the overlapping 'Case Else … End Select … refreshpolicylocation … If StrComp(strLevel,"POL")' block so the If/body indentation stays correct through End Sub; consequently sibling Case labels (Case "WOR" from IMG_4465 vs Case Else from IMG_4466) show a harmless indent mismatch. Similar minor indent-scale drift at the IMG_4469→4470 seam (mlngTimeBOProcessing region, 4-sp vs 8-sp base; IMG_4470 also re-renders that line's comment garbled — kept IMG_4469's cleaner two-line version) and at IMG_4470→4471 and IMG_4473→4474 seams. IMG_4474→IMG_4475 share no overlapping line but read as continuous (the note frames 4475 as continuing 4474); treated as continuous, no gap inserted, but a small unphotographed span there cannot be fully ruled out.

## Band 4476-4489  (first: continuation, last: UserOption)
**Gaps:** Gap between IMG_4476 and IMG_4477: the two photos share no overlapping lines and the transcript notes indicate a querystring-key extraction block was un-photographed. IMG_4476 ends with the completed "PolicyNum" block (its End If) and IMG_4477 opens directly on the "PolicyPremium" GetQueryStrSubStrWithValue block; the intervening blank line + `strTempQueryString = ""` (and possibly another key block) were skipped by the scroll.

**Low-confidence:** (none)

## Band 4490-4503  (first: continuation, last: RefreshSearchPageLists)
**Gaps:** IMG_4494 -> IMG_4495: no overlapping lines. IMG_4494 ends mid-body of Function PathDescription (just after "Set objDoc = document.frames(\"tree\")"), while IMG_4495 opens mid-body of a different (unnamed, tree-state) Sub that ends with objDoc.Tree_SetState(pblnState)/UpdateToolBar/End Sub. The remainder of PathDescription and the declaration+header of the following Sub were not photographed. A single GAP marker line was inserted at that seam.

**Low-confidence:** None. No illegible (guillemet-question-guillemet) markers appear in any transcript IMG_4490-IMG_4503. Note (not a code-legibility issue): IMG_4500 is a re-take showing the exact same screen as IMG_4499 (identical Function IsExternalWindowOpen body, same Ln 1405 cursor) so it contributed no unique lines; it was fully de-duplicated against IMG_4499. Also, several transcripts carry a tab-attribution note (Eebrowser.asp vs Main_ISLLSYS_20010101.vbs), which per instructions was ignored - content stitched as one continuous file.

## Band 4504-4517  (first: continuation, last: ApplySecurityPolicy)
**Gaps:** (none)

**Low-confidence:** IMG_4506: outer-level statements ("On Error Resume Next", "blnOpenWindows = False", "If StrComp(window.name...") appear flush-left in the transcript (sub-body indentation apparently lost in the photo); text is verbatim but indentation is uncertain. IMG_4507: the Else "policy window" branch has inconsistent/reduced indentation ("Dim blnIgnoreChanges" and "blnIgnoreChanges = False" flush-left); text verbatim, indentation uncertain. IMG_4510: nesting depth of the If/End If/End With staircase (the CallServer/RefreshSearchPageLists/Set objDoc = Nothing/End With block) is best-effort per the transcript's own medium-confidence note; line text is confident, exact indent columns are not. IMG_4514: one comment line was truncated at the photo edge ("...a querystring valu"); resolved using IMG_4513's complete copy ("...a querystring value"). No guillemet-question-guillemet illegible placeholders were present in any photo.

## Band 4518-4531  (first: continuation, last: ExecuteBrowserCommands)
**Gaps:** Gap between IMG_4520 and IMG_4521 (inside Sub ButtonOnClickHandler: photos jump from the ".marrEEData(5) = ""1""" EEData array setup to "End With"; the remaining array setup and the ".CallServer" invocation before End With were not photographed). Gap between IMG_4521 and IMG_4522 (inside Sub CalendarButtonOnClick: photos jump from the pre-modal comment "'-- a problem and the value is cleared the first time the control gets focus." to the post-modal "'--Check to see if the date was changed in the modal dialog--"; the code that stores the value into mvntRtnValue and opens the modal calendar dialog was not photographed). All other seams either overlapped (deduped) or abutted exactly with no missing content (IMG_4526->4527 completes the Sheridan tree block contiguously; IMG_4528->4529 continues the '--Clean Up-- block contiguously).

**Low-confidence:** IMG_4527: illegible span at the end of the '--IE8 Tree Control-- ElseIf condition, preserved verbatim as the «?» placeholder ("...AQSCONTROLLIBRARY.TREEVIEW"", vbTextCompare)«?»); by parallelism with the two ElseIf branches above it almost certainly continues " = 0 Then" off the right edge of the frame, but this was not legible. IMG_4529/IMG_4530: two-column comment-to-Dim alignment is ambiguous in the ExecuteBrowserCommands declaration block (the photos disagree on whether the comment "'in mxmlBrowserCtl() from the server." attaches to Dim intRtnCount or Dim intCount); the IMG_4529 layout was kept (comment attached to intRtnCount) and IMG_4530's overlapping intCount/intIndex re-render was deduped.

## Band 4532-4545  (first: continuation, last: continuation)
**Gaps:** (none)

**Low-confidence:** IMG_4537: two right-edge horizontal-scroll truncations marked with the «?» placeholder — the DISPLAY_INFORMATION showModalDialog feature string ends "...dialogWidth:400px; st«?»" and the DISPLAY_TAXCITY_INFORMATION one ends "...dialogWidth:650px«?»" (trailing feature-string args cut off). IMG_4538: one «?» truncation in the LOAD_COMBOS loop — getNamedItem("valu«?» (argument almost certainly "value" plus .nodeValue, tail cut off at screen edge). IMG_4541: one truncation marked with the ‹?› placeholder — the direct-navigate ExecuteAction call ends "...GetStringField(strAdd‹?›" (4th GetStringField argument and closing paren cut off at right edge). Note also: this band is a single continuous function body (ExecuteBrowserCommands, per the TrapBrowserError tags); no Sub/Function declaration line appears anywhere in it. Photos IMG_4540, IMG_4542, and the OPEN_MODAL continuation of IMG_4545 were framed/horizontally-scrolled so their leading indentation is captured with fewer/more leading spaces than the surrounding photos — the line CONTENT is verbatim and unambiguous, but the absolute indentation depth on those spans is a framing artifact and is not fully reliable.

## Band 4546-4559  (first: continuation, last: FillCallArray)
**Gaps:** Scroll gap between IMG_4553 and IMG_4554: the two photos share no overlapping lines. IMG_4553 ends at the SET_VISIBILITY case's '// Added for functional security comment, and IMG_4554 opens at Call GetSecurityValues(...); the intervening functional-security setup block (blnSecDisabled/blnSecVisible defaults, strProgramType/strPageMatchCode init, With objCallingWindow, GetActionMenuNameException, matchcode lookup) was skipped by the scroll.

**Low-confidence:** (none)

## Band 4560-4573  (first: FillComboList, last: GetControlValue)
**Gaps:** IMG_4564 -> IMG_4565: no overlapping lines at this seam. IMG_4564 ends by closing FillSelectList with 'End Sub'; IMG_4565 opens with the divider comment and 'Sub FocusFirstControl'. Content appears continuous (clean Sub boundary, nothing logically missing), but since the seam has zero overlap I inserted the marker '<<< GAP: possible unphotographed lines between IMG_4564 and IMG_4565 >>>' to flag possibly-skipped lines. All other seams (4560/61, 4561/62, 4562/63, 4563/64, 4565/66, 4566/67, 4567/68, 4568/69, 4569/70, 4570/71, 4571/72, 4572/73) had clean overlaps that were de-duplicated.

**Low-confidence:** IMG_4560: an isolated "--" token sits alone at the far right of an otherwise-blank line inside the combobox box-comment (after the '--for any <item> nodes to use for the list' lines); the transcript flags it as likely right-edge box-comment padding or a scroll/rendering artifact. Preserved verbatim. IMG_4563: the long xdiPageData.selectnodes XPath line is truncated at the photo's right edge, ending '...& objListItems(i<?>' where <?> marks the cut-off remainder of the expression. Preserved verbatim.

## Band 4574-4587  (first: continuation, last: GetXMLData)
**Gaps:** No overlapping lines between IMG_4577 and IMG_4578 (Dim block for GetXMLData is discontinuous: IMG_4577 ends at 'Dim strFilter', IMG_4578 resumes at 'Dim strItemXmlList'); No overlapping lines between IMG_4582 and IMG_4583 (IMG_4582 ends inside the 'If strListIndex <> "" Then' block at 'Call FillComboList(...)', IMG_4583 resumes at the outer 'End If / Not blnLimitToList' — the "X" SetControlValue call and inner End If were not photographed).

**Low-confidence:** No illegible guillemet-question-guillemet placeholders anywhere in the band. Minor indentation uncertainty at three seams where the two overlapping photos disagreed on leading whitespace and the later photo's (internally consistent) version was kept: IMG_4578/IMG_4579 ('set/reset this flag' comment and following lines rendered at 20/16 spaces in IMG_4578 vs 8 spaces in IMG_4579); IMG_4585/IMG_4586 ('Else' / 'Control not found' at 4/8 spaces in IMG_4585 vs 8/12 in IMG_4586); IMG_4586/IMG_4587 (SecDis/AppDis block at 16/20 spaces in IMG_4586 vs 12/16 in IMG_4587). Also note (verbatim, not illegible): the actionlist block in IMG_4586 has no visible 'End If' between 'objActionList.Add ...' and 'Set objControl = ...' — preserved as shown.

## Band 4588-4601  (first: continuation, last: NavButtonOnClick)
**Gaps:** (none)

**Low-confidence:** IMG_4601: the .ShowUserMessage(...) call argument list is truncated at the right editor edge; its final argument(s) after "WARNING", are illegible and preserved as the «?» marker.

## Band 4602-4615  (first: continuation, last: OnKeyPressHandler)
**Gaps:** (none)

**Low-confidence:** IMG_4612: five long lines are truncated at the right screen edge (word-wrap off) and end with the «?» illegible marker — the `If GetControlAttribute(objCurrentControl, "disabled") = "T" And ... And Len(mstrFocusChangedTo) =«?»` condition; the `ElseIf (StrComp(...combo...) = 0 or StrComp(...kpcombo...) = 0) And Len(mstrFocusCh«?»` condition; the two EOLA-related comment lines ending `...where the date contro«?»` and `...otherwise we get the ca«?»`; and the `If UCase(objCurrentControl.classid) <> "CLSID:CC696B63-4159-11D0-BDCB-0020A90B183A" And UCase(objPreviousControl.classid) <> "CLSID:CC696B63-4159-1«?»` condition. Also, transcriber-flagged medium confidence on IMG_4608/4609 (photo skew / reconstructed indentation) and a slight indentation discontinuity at the 4607→4608 and 4613→4614 seams where the two photos disagree on indent depth for the shared/adjacent lines.

## Band 4616-4629  (first: continuation, last: SetInitialValueData)
**Gaps:** (none)

**Low-confidence:** IMG_4627/IMG_4628 seam: after the shared `else` + three `.marrEEData` assignment block, the next source line is transcribed inconsistently — IMG_4627 reads it as `        end if` (8-space) while IMG_4628 reads it as `    else` (4-space); I kept IMG_4628's `    else` because that photo carries the continuation into `Case "PASSWORD", "FILE"`, so IMG_4627's `end if` was treated as the misread of the same line (not an extra line). Also, the INPUT/TEXT branch here has irregular VBScript (multiple lowercase `else`/`end if` with a missing `End If` for the `If Len(.marrEEData(2)) > 0` block) preserved verbatim as photographed. Separately, an indentation-regime shift occurs mid-Sub SetArrayData at the IMG_4625→IMG_4626 seam: IMG_4622-4625 were transcribed with deeper indentation (Case labels at 8 spaces) while IMG_4626-4629 are shallower (Case labels at 0 spaces); code content is continuous but absolute indentation differs by photo batch (perspective/zoom artifact). Minor perspective indentation differences were also reconciled at the 4619/4620, 4620/4621, and 4624/4625 seams (kept the internally-consistent version). No illegible/guillemet-marked spans present in this band.

## Band 4630-4643  (first: SetInitialValueData, last: SetControlAttribute)
**Gaps:** (none)

**Low-confidence:** (none)

## Band 4644-4657  (first: continuation, last: ShowZeroText)
**Gaps:** IMG_4650 -> IMG_4651: the two photos share no overlapping line and content was skipped. Missing between them: the COMBO/KPCOMBO 'Case "L"' branch and the opening of its 'Case "X"' (specifically the 'If objControl.ShowZero = "T" Then' header); IMG_4651 resumes inside that Then-branch at 'objControl.listindex = vntValue'. Marker inserted after 'Select Case strVarType' under Case "COMBO", "KPCOMBO". | IMG_4656 -> IMG_4657: no overlapping line; the processor-creation statement (e.g. 'Set xmlProcessor = xmlTemplate.createProcessor()') between TrapBrowserError("BuildTab|4") and TrapBrowserError("BuildTab|5") was not photographed. Marker inserted between those two Call lines in Sub BuildTab.

**Low-confidence:** Leading indentation was normalized to consistent 4-space logical levels because the photo groups used conflicting horizontal-scroll baselines (IMG_4644 clamped indentation toward column 0; IMG_4645-4649 rendered Subs at column 4; the VS2022 photos IMG_4650-4657 used a shallower baseline). Line TEXT is verbatim; only leading whitespace was reconciled at seams. | IMG_4644 vs IMG_4645 disagree on one comment: '...ANY control on ANY tab. It is' (single space, IMG_4644) vs '...ANY tab.  It is' (double space, IMG_4645); the IMG_4645 double-space form was kept. | Verbatim quirks preserved as photographed, not corrected: mixed-case 'UCASE(objControl.type)' in the PASSWORD test (IMG_4647); lowercase 'objControl.checked = false' in the "X" branches (IMG_4650/IMG_4652/IMG_4653); lowercase 'if/elseif/end if/for/next' in the numeric-mask block (IMG_4652); the space inside the XPath '//control [ @matchcode = ...' (IMG_4655); and the non-standard escape tokens '#http39;/#http60;/#http62;/#http38;' in UnEscapeTextValueForXML (IMG_4657). | Indentation of the post-gap COMBO 'Case "X"' lines (IMG_4651) and of the internally-inconsistent nested-If block under INPUT/'Case "TEXT..."' (IMG_4651-4652) was reconstructed from block logic rather than the noisy transcribed columns.

## Band 4658-4660  (first: ShowZeroText, last: ClearXMLDetail)
**Gaps:** (none)

**Low-confidence:** (none)

