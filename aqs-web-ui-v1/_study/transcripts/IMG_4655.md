---
photo: IMG_4655.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26 -- unchanged from prior photos in this run)
orientation: 0
confidence: medium
notes: >
  Same VS session/tabs as IMG_4650-4654. IMPORTANT FINDING: the internal
  TrapBrowserError() calls literally tag this routine as "EEBrowser|SetControlValue|1|"
  and "EEBrowser|SetControlValue|2|", and the sub ends with "End Sub" right after. This
  confirms (a) the sub being transcribed across IMG_4650-4655 is Sub SetControlValue,
  and (b) it is logically part of the "EEBrowser" module even though the active editor
  tab reads "Main_ISLLSYS_20010101.vbs" -- strongly suggesting Main_ISLLSYS_20010101.vbs
  is a client-specific (ISLLSYS) dated (20010101) COPY/OVERRIDE/FORK of Eebrowser that
  still carries the original "EEBrowser" self-identifying string labels used for error
  logging. This is exactly the kind of "Main.vbs"-pattern file the task asked to watch
  for, though it now looks more like a per-client variant of Eebrowser rather than an
  unrelated top-level frame controller. Immediately after "End Sub" and a comment-dash
  divider line, a NEW subroutine begins: "Sub BuildTab(ByRef objCallingWindow)" -- reads
  tabstrip.matchcode/selectedTab from objCallingWindow, builds a debug string, and has
  an early-exit "If Len(intIndex) = 0" branch calling TrapBrowserError("BuildTab|1.a").
---

```vbs
    'If the control that just lost focus is having its value changed by a browser command,
    'need to reset the inital value indicator to prevent post processing.
    If StrComp(mstrPreviousMatchcode, objControl.name, vbTextCompare) = 0 Then
        'this is used by the onfocushandler
        mblnResetInitialValue = True
    End If

        Call TrapBrowserError("EEBrowser|SetControlValue|1|" & objControl.id)
    End If

    If Len(strMatchCode) > 0 Then
        With objCallingWindow
            Set nodControl = .mxmlDoc.selectSingleNode("//control [ @matchcode = '" & strMatchCode & "']")
            If Not nodControl Is Nothing And Not IsEmpty(nodControl) Then
                If StrComp(strVarType, "L", vbTextCompare) = 0 Then
                    nodControl.attributes.getNamedItem("text").nodeValue = vntValue
                End If
            End If
        End With
        Call TrapBrowserError("EEBrowser|SetControlValue|2|" & strMatchCode)
    End If

End Sub
'----------------------------------------------------------------------------------------


Sub BuildTab(ByRef objCallingWindow)

    Dim intIndex
    Dim strMatchCode
    Dim strDebugText

    On Error Resume Next

    With objCallingWindow
        strMatchCode = .tabstrip.matchcode
        intIndex = CInt(.tabstrip.selectedTab)
        strDebugText = "[ tabnumber = " & CStr(intIndex) & " ] - [ matchcode = " & strMatchCode & " ]"

        If Len(intIndex) = 0 Then
            If mblnDebug Then window.status = "no index"
            Call TrapBrowserError("BuildTab|1.a")
            Exit Sub
        End If
```
