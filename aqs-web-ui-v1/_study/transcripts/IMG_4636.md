---
photo: IMG_4636.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible in editor; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4635, same tab "Main_ISLLSYS_20010101.vbs"
  (Eebrowser.asp inactive tab still present). Continues
  Case "CLSID:CC696B63-4159-11D0-BDCB-0020A90B183A" (Protoview Date/Time
  Control): Select Case strAttribute -> Case "VISIBLE" (style.visibility
  visible/hidden, then TrapBrowserError ...|SetControlAttribute|2), then
  Case "DISABLED" with a substantial "EOLA fix" comment block: creates a
  tempControl via objCallingWindow.document.all(objControl.id) because "the
  control that is being passed in can't be updated", sets
  tempControl.Enabled = Not vntValue; then looks for a related calendar
  command button (objCallingWindow.document.all("cal" & .name)) and -- per an
  explicit ALL-CAPS comment "KEEP THE CALENDAR BUTTON ENABLED - PREVENT THE
  SHOWMODAL INSTEAD" -- recursively calls
  SetControlAttribute(objRelatedControl,"",strAttribute,strOrigValue,
  objCallingWindow) on it; then sets tempControl.backcolor via RGB() using the
  module-level mintDisabledRed/Green/Blue (if vntValue) or RGB(255,255,255)
  (else), each step bracketed by TrapBrowserError checkpoints (|3.1, |3.2,
  |3.3). End Select closes strAttribute (comment 'strAttribute), End Select
  implicit for classid case ends here too. New Case begins:
  "CLSID:C2000000-FFFF-1100-8200-000000000004" (comment: Protoview Numeric
  Control), with its own Select Case strAttribute / Case "VISIBLE" beginning.
  NOTE: the editor showed left-margin change/modification indicator marks
  (small vertical bar, rendered in the photo as faint tick marks) next to the
  "Set objRelatedControl", "If Not objRelatedControl Is Nothing Then", and
  "End If" lines -- these are editor UI decorations, not literal source
  characters, and are NOT included in the transcription below.
---
```vbs
'--Protoview Date/Time Control--
Select Case strAttribute
    Case "VISIBLE"
        If vntValue Then
            .style.visibility = "visible"
        Else
            .style.visibility = "hidden"
        End If
        Call TrapBrowserError("EEBrowser|SetControlAttribute|2")

    Case "DISABLED"
        'EOLA fix:  the control that is being passed in can't be updated so create a temp one that can.
        Dim tempControl
        Set tempControl = objCallingWindow.document.all(objControl.id)
        tempControl.Enabled = Not vntValue

        'Look for a command button related to the date control
        'that is used for displaying a calendar behavior.  If found
        'set its DISABLED attribute to match the Date Control's.
        '***** KEEP THE CALENDAR BUTTON ENABLED - PREVENT THE SHOWMODAL INSTEAD.
        Set objRelatedControl = objCallingWindow.document.all("cal" & .name)
            Call TrapBrowserError("EEBrowser|SetControlAttribute|3.1")
        If Not objRelatedControl Is Nothing Then
            Call SetControlAttribute(objRelatedControl,"",strAttribute,strOrigValue,objCallingWindow)
        End If  'Not objRelatedControl Is Nothing

        Call TrapBrowserError("EEBrowser|SetControlAttribute|3.2")

        If vntValue Then
            tempControl.backcolor = RGB(mintDisabledRed, mintDisabledGreen, mintDisabledBlue)
        Else
            tempControl.backcolor = RGB(255, 255, 255)
        End If
        Call TrapBrowserError("EEBrowser|SetControlAttribute|3.3")

End Select
    'strAttribute

Case "CLSID:C2000000-FFFF-1100-8200-000000000004"
    '--Protoview Numeric Control--
    Select Case strAttribute
        Case "VISIBLE"
            If vntValue Then
```
