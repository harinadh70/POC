---
photo: IMG_4568.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4567, same Visual Studio window (solution AQS.Advantage.BatchEdit), same tabs
  "Main_ISLLSYS_20010101.vbs" (inactive) / "Eebrowser.asp" (active). Continues the GetControlAttribute header comment
  (VISIBLE/DISABLED attribute notes, PARAMETERS section), then function body: Dim blnBooleanAttribute, initializes
  GetControlAttribute = "", uppercases strAttribute, then `With objControl / Select Case UCase(.tagname) / Case "OBJECT"
  / Select Case UCase(.classid)` with a Case listing several CLSID GUIDs (Protoview Date/Time, Numeric Control OR Tree
  control), nested `Select Case strAttribute / Case "VISIBLE" / Select Case UCase(.style.visibility)` handling
  VISIBLE/HIDDEN. Windows taskbar visible at bottom of screen with git branch indicator reading "develop". No
  AQSMain/Main.vbs body content visible (that tab still inactive). No line-number gutter.
---
```vbscript
        - "VISIBLE", Boolean, returned values "T"/"F"/""
        - "DISABLED", Boolean, returned values "T"/"F"/""

    Since these attribute directly affect either standard HTML or ActiveX
    attributes (or properties), these attributes are not required to be
    included in the HTML before manipulating them. However, if ANY USER
    DEFINED or CUSTOM attributes are adjusted in the future, they MUST BE
    present in the HTML BEFORE they can be manipulated with this routine.
'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
'
'PARAMETERS:
'  objControl: An object reference to the desired control.
'strAttribute: The attribute name to get.
'
'******************************************************************************
On Error Resume Next

Dim blnBooleanAttribute                'Flag indicating the attribute is a boolean type.

'--Assume an empty string for a returned value--
GetControlAttribute = ""

'--Condition the in-coming parameters--
strAttribute = UCase(strAttribute)
Call TrapBrowserError("EEBrowser|GetControlAttribute|1")

With objControl
    Select Case UCase(.tagname)
        Case "OBJECT"
            Select Case UCase(.classid)
                Case "CLSID:CC696B63-4159-11D0-BDCB-0020A90B183A","CLSID:C2000000-FFFF-1100-8200-000000000004","CLSID:1C203F13-95AD-11D0-A84B-00A0247B735B"
                    '--Protoview Date/Time, Numeric Control OR Tree control--
                    Select Case strAttribute
                    Case "VISIBLE"
                        Select Case UCase(.style.visibility)
                        Case "VISIBLE"
                            GetControlAttribute = "T"
                        Case "HIDDEN"
                            GetControlAttribute = "F"
                        End Select
                            'UCase(.style.visibility)
```
