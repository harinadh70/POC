---
photo: IMG_4510.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible)
orientation: 0
confidence: medium
notes: >
  Continuation of IMG_4509, finishes the Cancel-routine block and the rest of
  Sub window_onunload: closes with a "focus the opener window" routine (uses
  mobjParentWindow / mblnDoNotSetFocusToParent), a TrapBrowserError("window_onunload|1")
  call, then a cleanup block nulling out module-level state
  (mobjParentWindow, mdicChildWindows, mdicNonPolicyChildWindows,
  mdicExternalWindows, mxmlBatchEdits, mxmlStyle, mxmlProcessor) and "End Sub".
  A dashed divider follows, then a NEW sub begins: "Sub
  ProgressBarTime_SetValue(mstrText)" with banner comment "Support routine to
  set the timing for a progress modal". This is a plain support/utility sub,
  not another window lifecycle event handler -- suggests we are now past the
  window_onload/onbeforeunload/onunload cluster and into general helper subs
  within the same Main_ISLLSYS_20010101.vbs file. Same Find-in-Files box still
  shows stale search text "Sub window_onbeforeunload" (unchanged since
  IMG_4506) -- confirms this is scrolling through the same file, not a new
  search result. CAVEAT (confidence downgraded to medium for this reason):
  lines 26-39 of the code block below are a deeply-nested staircase of
  If/End If/End With statements continuing from IMG_4509's With objDoc block;
  text content per line is verbatim and high-confidence, but the exact
  indentation depth of each line is a best-effort reading (photo angle/blur
  made precise column measurement difficult) and should not be relied on for
  reconstructing exact nesting. Lines 42 onward (the "set focus to opener"
  routine and cleanup block) are clearly legible with confident indentation.
---
```vb
                        'Set post-process indicator
                    If Not mblnPolicyDiscard  Then
                        Call .CallServer( .document.all("dtaCancel"), False)
                    End If
                    Call RefreshSearchPageLists
                End If
                End If
            Set objDoc = Nothing
        End If
    End With

        End If
    End If
End If
'--------------------------------------------------

    'This routine attempts to set focus to the page that opened it.
    If Not mobjParentWindow Is Nothing Then
        If mblnDoNotSetFocusToParent = false Then
            If Not mobjParentWindow.closed Then
                mobjParentWindow.focus
            End If
        End If
    End If

    Call TrapBrowserError("window_onunload|1")
    'Clean up
    Set mobjParentWindow = Nothing
    Set mdicChildWindows = Nothing
    Set mdicNonPolicyChildWindows = Nothing
    Set mdicExternalWindows = Nothing
    Set mxmlBatchEdits = Nothing
    Set mxmlStyle = Nothing
    Set mxmlProcessor = Nothing

End Sub
'--------------------------------------------------------------------------------------

Sub ProgressBarTime_SetValue(mstrText)
'****************************************************************************
' Support routine to set the timing for a progress modal
'****************************************************************************

    On Error Resume Next
```
