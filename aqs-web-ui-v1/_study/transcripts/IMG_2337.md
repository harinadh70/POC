---
photo: IMG_2337.JPG
type: vscode-code
file: aqs-web-ui/src/features/auth/utils/middleware.ts
lines: 14-45
orientation: 180
confidence: high
notes: Same file/tab as IMG_2336 (middleware.ts, features/auth/utils), scrolled to show the rest of the file down to EOF (line 45 blank). Sticky-scroll header shows line 14 (export const authMiddleware...); body resumes fully at line 15. Confirms line 34 comment begins with a red "❌" emoji and line 42 comment begins with a "✔️" (checkmark) emoji. Explorer sidebar: middleware.ts (features/auth/utils, badge "5") highlighted, siblings action.ts, loader.ts above, another middleware.ts (features-level) below. Status bar: aqs-web-ui, hitanshu/experimental*, 30 errors / 0 warnings, No Solution, Ln 1 Col 1, Tab Size 4, UTF-8, CRLF, TypeScript. Time overlay 4:46 PM 7/10/2026.
---
14: export const authMiddleware: MiddlewareFunction = async ({ request, context }, next) => {
   [sticky-scroll header repeats line 14]
15:     // Check if user was already fetched and stored in context
16:     let userInfo = context.get(userContext);
17:
18:     if (!userInfo || !userInfo.userId) {
19:         // Try to get sessionInformation from localStorage using typed utility
20:         try {
21:             const sessionInfo = getItem<SessionInfo>('sessionInformation');
22:             if (sessionInfo && sessionInfo.userId) {
23:                 userInfo = sessionInfo;
24:                 // Store in context for further use
25:                 context.set(userContext, sessionInfo);
26:             }
27:         } catch (error) {
28:             console.error('Error getting sessionInformation from localStorage', error);
29:         }
30:     }
31:
32:     const url = new URL(request.url);
33:
34:     // ❌ if invalid userInfo kick out
35:     if (!userInfo || !userInfo.userId) {
36:         if (url.pathname !== '/') {
37:             console.log('No valid user, redirecting to login');
38:             throw redirect('/');
39:         }
40:     }
41:
42:     // ✔️ go ahead
43:     await next();
44: };
45:
