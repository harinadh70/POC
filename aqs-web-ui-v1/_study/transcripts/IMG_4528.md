---
photo: IMG_4528.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter line numbers shown; status bar reads Ln 2871, Ch 26 -- unchanged)
orientation: 0
confidence: high
notes: >
  Directly continues IMG_4527. Right after "End Select" comes the "update the list if this
  control failed the test" block: builds strControlList from each failed control's associated
  label (.document.all("lbl" & objControls(intIndex).name).innertext), comma-joining multiple
  entries; two TrapBrowserError checkpoints "EEBrowser|CheckSpecifiedIndicator|1" and
  "EEBrowser|CheckSpecifiedIndicator|2" (EEBrowser-prefixed labels again, same pattern seen in
  IMG_4521's CalendarButtonOnClick despite living in Main_ISLLSYS_20010101.vbs). Closes out all
  nested Ifs/For loop (Next) and "End With". CONFIRMS this whole block (IMG_4522-4528) is the
  body of Function CheckSpecifiedIndicator: "'--Set the return value--" /
  "CheckSpecifiedIndicator = blnSuccess" then "'--Clean Up--" begins as the last legible line
  at the bottom of this frame (continues into IMG_4529: Set objControls/objLabel/colXMLLists =
  Nothing, End Function, then new Sub ExecuteBrowserCommands begins). This proves the "Sub
  window_onbeforeunload" seen in the class-member navigator on IMG_4526/4527 was stale/not
  reflecting the visible code. Title bar clock visible bottom-right: 8:53 PM, 7/14/2026
  (taskbar). ADMIN badge visible top-right of VS window (not present in earlier photos of this
  session).
---
```vbs
                    End Select
                        'UCase(objControls(intIndex).tagname)

                    ' Before next item, update the list if this control failed the test
                            '--Add this control's label text to the returned string--
                            '--listing all the controls that failed.    If needed.  --
                        If blnUpdateList Then
                            Set objLabel = .document.all("lbl" & objControls(intIndex).name)
                            ' Make sure there is a label
                            If Not objLabel Is Nothing Then
                                If Len(strControlList) = 0 Then
                                    'This is the first label to go into the string.
                                    strControlList = objLabel.innertext
                                        Call TrapBrowserError("EEBrowser|CheckSpecifiedIndicator|1")
                                Else
                                    'Len(strControlList) <> 0
                                    'This is the 2nd or greater label to go into the string.
                                    strControlList = strControlList & ", " & objLabel.innertext
                                        Call TrapBrowserError("EEBrowser|CheckSpecifiedIndicator|2")
                                End If
                                    'Len(strControlList) = 0
                            End If
                        End If
                            'blnUpdateList
                        blnUpdateList = False
                            'Reset Flag

                End If
                    'StrComp(GetControlAttribute(objControls(intIndex), "VISIBLE"), "T", vbTextCompare) = 0
            End If
                'StrComp (objControls(intIndex).getAttribute(strIndicator),"1",vbTextCompare ) = 0
        End If
            'Not IsNull(objControls(intIndex).getAttribute(strIndicator))
        Next
            'intIndex

    End With
        'objCallingWindow

    '--Set the return value--
    CheckSpecifiedIndicator = blnSuccess

    '--Clean Up--
```
