---
photo: IMG_4620.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Same Visual Studio session; active tab (gold highlight) confirmed as
  Main_ISLLSYS_20010101.vbs. Finishes Sub PreProcess from IMG_4619: closes
  the COMBO/KPCOMBO branch (objControl.select), adds a "<SELECT> tag
  Pre-Process" branch calling .CallServer(objControl, True), then outside the
  mblnCallServer block calls SetInitialValueData when the matchcode changed,
  records .mstrPreviousMatchcode, and resets .mblnCallServer when the current
  control equals .mstrErrorMatchcode (comments describe this as clearing a
  module-level flag set only when the server requested a "DISPLAY_ERROR"
  verb). Ends with TrapBrowserError("EEBrowser|PreProcess|4") and End Sub --
  again an "EEBrowser"-labeled call inside the Main file. Then begins Sub
  RunXMLBrowserProcedures(ByRef strCallType, ByRef objCallingWindow), purpose
  comment: "will perform and XSL query against the XML data island on the..."
  (cut off at bottom of frame). Windows taskbar visible at very bottom of
  photo shows clock "9:09 PM 7/14/2026" -- a photo-metadata artifact, not
  code.
---
```vbscript
            ElseIf objControl.listcount = 0 Then
                Call FillComboList (objControl, objCallingWindow)
            End If

            objControl.select
        End If
            'UCase(objPreviousControl.tagname) = "COMBO" or UCase(objControl.tagname) = "KPCOMBO"

        '--<SELECT> tag Pre-Process--
        If UCase(objControl.tagname) = "SELECT" Then
            Call .CallServer(objControl, True)
        End If
            'UCase(objPreviousControl.tagname) = "SELECT"

    End If
        'mblnCallServer

    If StrComp(.mstrPreviousMatchcode, objControl.name, vbTextCompare) <> 0 Then
        Call SetInitialValueData(objControl, objCallingWindow)
    End If

    '--Remember the current control as the previous control--
    .mstrPreviousMatchcode = objControl.name

    '--If we are at the "error" control, then reset the module-level flag.--
    '--NOTE: Then flag is set ONLY if the server requested the browser to--
    '--perform the "DISPLAY_ERROR" action (verb).
    If objControl.name = .mstrErrorMatchcode Then
        .mblnCallServer = True
            'reset the flag
    End If  'objControl.name = mstrErrorMatchcode
End With
    'objCallingWindow

Call TrapBrowserError("EEBrowser|PreProcess|4")

End Sub
'------------------------------------------------------------------------------

Sub RunXMLBrowserProcedures(ByRef strCallType, ByRef objCallingWindow)
'*****************************************************************************
'PURPOSE:
'This routine will perform and XSL query against the XML data island on the
```
