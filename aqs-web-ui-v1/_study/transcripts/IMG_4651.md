---
photo: IMG_4651.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26 -- same as IMG_4650, view has scrolled not cursor-moved)
orientation: 0
confidence: medium
notes: >
  Same Visual Studio window/session as IMG_4650 (same two tabs: Main_ISLLSYS_20010101.vbs
  active/gold-pinned, Eebrowser.asp inactive/blue; same cursor Ln:2871). This is a direct
  continuation of the SetControlValue-style dispatcher: end of a COMBO listindex-shift
  block (with comment about combobox having no "real" zero element), then Case "IBUTTON"
  (sets objControl.text for "L","X" var types, objControl.value for "V"), then Case
  "INPUT" -> Select Case UCase(objControl.type) -> Case "TEXT","PASSWORD","BUTTON",
  "SUBMIT","HIDDEN" with decimals/iscalendar attribute checks, date-cleanup logic
  (00/00/0000, 12/31/1899 sentinel dates) and objCallingWindow.CheckDate(vntValue) call.
  Same file-attribution ambiguity as IMG_4650 applies (see that transcript's notes).
---

```vbs
                    objControl.listindex = vntValue
                Else
                    'objControl.ShowZero = "F"
                    'Since there is NO "real" zero element, we need to shift the
                    'index value down one to match the element in the combobox.
                    'Sets the "X" variable
                    objControl.listindex = vntValue - 1
                End If
        End Select
            'strVarType

    Case "IBUTTON"
        Select Case strVarType
            Case "L","X"
                'Sets the "L" variable
                objControl.text = vntValue
            Case "V"
                'Sets the "Value" property
                objControl.value = vntValue
        End Select
            'strVarType

    Case "INPUT"
        Select Case UCase(objControl.type)
            Case "TEXT", "PASSWORD", "BUTTON", "SUBMIT", "HIDDEN"
                If IsNull(objControl.getAttribute("decimals")) Or Trim(vntValue) = "" Then
                        If IsNull(objControl.getAttribute("iscalendar")) Then
                    If Trim(vntValue) = "00/00/0000" Then
                        vntValue = ""
                    End If
                            objControl.value = Trim(vntValue)
                            'Sets the "L" variable
                        Else
                            ' calendar control
                            If Not objCallingWindow.CheckDate(vntValue) Then
                                vntValue = ""
                            ElseIf Trim(vntValue) = "12/31/1899" OR Trim(vntValue) = "00/00/0000" Then
                                vntValue = ""
                            End If
                            objControl.value = vntValue
                    End If

                Else
```
