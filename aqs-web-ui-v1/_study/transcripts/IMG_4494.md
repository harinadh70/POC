---
photo: IMG_4494.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 1405, Ch 4, Col 13)
orientation: 0
confidence: high
notes: IDE is full Visual Studio (not VS Code) showing "AQS.Advantage.BatchEdit" project / "AQS.Advantage" solution. TWO tabs open: "Main_ISLLSYS_20010101.vbs" (inactive, tan/pinned-looking tab, left) and "Eebrowser.asp" (ACTIVE tab, blue highlight, content shown below is from this file). NOTE: filename extension here is .asp, not .vbs as in the earlier Eebrowser.vbs transcription -- flag as possibly a different/related file, or same logical file under a different name/extension. Also of note: "Main_ISLLSYS_20010101.vbs" tab name strongly resembles a top-level "Main*.vbs" frame-controller script per the task's watch-list, but its content was NOT visible in this photo (only the tab label). Code shown is functions SelectedNodeKey() and PathDescription(...) -- tree-node helper functions, thematically consistent with Eebrowser's tree browser purpose. No line-number gutter is visible in this editor view.
---
```vbs
Function SelectedNodeKey()
'*******************************************************************************
' Returns the key of the selected tree node
'*******************************************************************************
    Dim objDoc
    On Error Resume Next

    SelectedNodeKey = ""

    Set objDoc = document.frames("tree")

    If Not objDoc Is Nothing Then
        SelectedNodeKey = document.frames("tree").SelectedNodeKey
        Set objDoc = Nothing
    End If

    Call TrapBrowserError("SelectedNodeKey|1")

End Function
'--------------------------------------------------------------------------------

Function PathDescription(ByVal pstrNodeKey, ByVal pintStartLevel, ByVal pintEndLevel)
'*******************************************************************************
    'This function returns the text of the tree nodes starting at the node with the
    'key of strNodeKey and moving up the branch till the level of intEndLevel (default=1).
    'If intStartLevel is a value other than 0, then the text for nodes greater
    'than intStartLevel will not be included.

    'Example: BUSINESSOWNERS  WI - Loc#1  Bld #1 - SHOE STORE

    ' This example could have been obtained from:
    ' 1. the building node with default intStartLevel
    '   2. the coverage node with intStartLevel = 3
'*******************************************************************************

    Dim objDoc

    On Error Resume Next

    Set objDoc = document.frames("tree")
```
