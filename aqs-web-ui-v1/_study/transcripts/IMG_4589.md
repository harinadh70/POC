---
photo: IMG_4589.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers visible; editor cursor status bar shows "Ln: 2871, Ch: 26")
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4588 (same VS window/tab; background pinned tab still
  reads "Main_ISLLSYS_20010101.vbs", content unseen). Finishes the mxmlBrowserCtl
  setup (async=False, preserveWhiteSpace=False, loadXML objNodes.xml), calls
  ExecuteBrowserCommands("", objCallingWindow) with an Exit Sub guard if
  objCallingWindow becomes Nothing, then removes the browser-commands node from the
  XML doc. Closes with "End If" / "End With 'objCallingWindow" -- this End With
  matches a "With objCallingWindow"-style block that must have opened much earlier
  (not seen in this batch). Then a "'--Clean Up--" section nulls out objNodes,
  objNamedNodeMap, objNamedNodeMapLI, objNode, objControl, objListItems, objDoc,
  objParent, calls TrapBrowserError("EEBrowser|GetXMLData|13"), and "End Sub" --
  this is the END of the GetXMLData sub (TrapBrowserError call-site counter reached
  |13|, the highest seen). Immediately after, a NEW sub begins:
  "Sub SetRequiredIndicator(ByRef objControl, ByRef objCallingWindow)" with header
  comment block "PURPOSE: Add preceeding '*' to requird field labels" [sic, both
  "preceeding" and "requird" misspelled in source], "On Error Resume Next", and
  Dim objLabel / Dim strColor. This SetRequiredIndicator sub was referenced by name
  earlier in this batch (IMG_4584 notes mention a SetRequiredIndicator call); this is
  its actual definition. Still no Main.vbs/AQSMain.vbs file CONTENT visible in this
  batch -- only its tab label in the background throughout IMG_4581-4589.
---

```vbs
        Set .mxmlBrowserCtl = CreateObject("MSXML2.DOMDocument.6.0")
        .mxmlBrowserCtl.async = False
        .mxmlBrowserCtl.preserveWhiteSpace = False
        .mxmlBrowserCtl.loadXML objNodes.xml

        Call TrapBrowserError("EEBrowser|GetXMLData|11")
    'Have the browser run these commands
    Call ExecuteBrowserCommands("",objCallingWindow)
    If objCallingWindow Is Nothing Then
        Exit Sub
    End If

    ' Remove the browser commands
    Set objParent = objNodes.parentNode
    Set objNode = objParent.removeChild (objNodes)

    End If

End With
    'objCallingWindow


'--Clean Up--
Set objNodes                   = Nothing
Set objNamedNodeMap    = Nothing
Set objNamedNodeMapLI = Nothing
Set objNode                    = Nothing
Set objControl              = Nothing
Set objListItems            = Nothing
Set objDoc                     = Nothing
Set objParent                = Nothing
Call TrapBrowserError("EEBrowser|GetXMLData|13")
End Sub
'----------------------------------------------------------------------------------------

Sub SetRequiredIndicator(ByRef objControl, ByRef objCallingWindow)
    '****************************************************************************
    'PURPOSE: Add preceeding '*' to requird field labels
    '****************************************************************************
    On Error Resume Next

    'Handle to a selected control's label - so it's text can be modified
    Dim objLabel
    Dim strColor
```
