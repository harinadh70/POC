---
photo: IMG_4459.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter line numbers visible in this VS view)
orientation: 0
confidence: high
notes: >
  Continues directly from IMG_4458 (same Visual Studio window, same active tab
  Main_ISLLSYS_20010101.vbs, inactive tab Eebrowser.asp). Shows the full body of
  Sub RefreshTree() (confirms objDoc.Build_Tree("POL|POL|0|", "LOADTREE") call
  against document.frames("tree")), then a blank/divider, then the start of
  Sub DeleteAction(ByVal pstrNodeKey, ByVal pstrAction) with its purpose header
  comment and a long block of Dim declarations. This is further confirmation that
  Main_ISLLSYS_20010101.vbs is a distinct frame-controller/tree-management script
  separate from Eebrowser.vbs. No line-number gutter visible.
---
```vbs
    Sub RefreshTree()
        Dim objDoc
        On Error Resume Next
        Set objDoc = document.frames("tree")
        Call objDoc.Build_Tree("POL|POL|0|", "LOADTREE")
        Set objDoc = Nothing
        Call TrapBrowserError ("RefreshTree|1")
    End Sub


'------------------------------------------------------------------------


Sub DeleteAction(ByVal pstrNodeKey, ByVal pstrAction)
'*****************************************************************************
'PURPOSE:
'  This subroutine calls the asp page that deletes the specified node from
'  the policy.  If the return value of result is Success, then the node
'    is deleted from the tree.
' **** Special Code is contained herein that deletes addional nodes
' **** Depending upon the conditions.

'*****************************************************************************
On Error Resume Next

    Dim objResponse                    ' XML document to hold the xml of the response

    Dim objNode                        ' node within ("/navigate") from response

    Dim strQueryString
    Dim objDoc
    Dim objFrame

    Dim objTreeNode
    Dim objParentNode
    Dim intStart
    Dim intEnd
    Dim intCount
    Dim strStateCode
    Dim blnStateFound

    Dim strLOB
    Dim strLevel
    Dim strNEXPNUM
```
</content>
