---
photo: IMG_4440.JPG
type: vbs-archive
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter shown)
orientation: 0
confidence: high
notes: >
  Same Visual Studio session/tabs. Top of visible region repeats the tail of IMG_4439
  (Dim objWindowNonPol / arrNonPolKeys, AddWindowToCollection window, screen-resolution
  resize block) — included once here for continuity/context, no ambiguity since this is
  single-column executable code (no comment-pairing issue). Inside Sub window_onload:
  after the resize block, creates GlobalVars (New globalVarsClass), sets mobjAQSMain =
  window, sets mobjParentWindow = window.opener, then enters a clearly-delimited
  "REPLACEMENT SEARCH PAGE" section (marked with a ">>>>>[begin section]>>>>>" banner
  comment) handling mblnReplacementHomePage, copying child-window references from the
  parent policy window via mdicChildWindows Keys/Items and LoadWindowReferences. High
  confidence throughout — this is plain sequential code, not the earlier
  declaration-with-aligned-comment layout. Still no line-number gutter visible.
---
```vbscript
    Dim objWindowNonPol
    Dim arrNonPolKeys

    AddWindowToCollection window

    '// resize window if resolution less than 1024 x 768
    If screen.width < 1024 And screen.height < 768 Then
        window.moveTo 0,0
        window.resizeTo screen.availWidth,screen.availHeight
    End If

    ' Create object to hold selected system variables
    Set GlobalVars = New globalVarsClass

    'Set a reference to this window.
    Set mobjAQSMain = window

    'Set a reference to the parent window. This allows navigation back.
    Set mobjParentWindow = window.opener

    ' >>>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>>>>>>>
    '                                                REPLACEMENT SEARCH PAGE

    '  If the QueryString("replacement") is any value,
    '    mblnReplacementHomePage is True
    ' This occurs when the user closes the searchpage while a policy is open.
    ' Then clicks the Home button.

    If mblnReplacementHomePage Then
        'Get the child window references from the policy window that is
        'opening this replacement search window.
        arrKeys = mobjParentWindow.mdicChildWindows.Keys
        arrWindows = mobjParentWindow.mdicChildWindows.Items
        For intCount = 0 To mobjParentWindow.mdicChildWindows.Count - 1
            'Only add references to policy windows,
            'not other child window, ie, batchedits, overrides...
            If StrComp(Left(arrKeys(intCount),6),"policy",vbTextCompare) = 0 Then
                Call LoadWindowReferences (arrKeys(intCount), arrWindows(intCount))
            End If
        Next

        'Now update the parent window property of all the child windows
        'so they know about this new search window.
```
