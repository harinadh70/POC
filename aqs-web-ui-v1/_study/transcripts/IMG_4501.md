---
photo: IMG_4501.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; status bar shows Ln 1405, Ch 4, Col 13; zoom not visible)
orientation: 0
confidence: high
notes: >
  Active tab is "Main_ISLLSYS_20010101.vbs" (tan/pinned background, bold black text); "Eebrowser.asp"
  is the plain inactive tab (dark navy, thin light text) -- same pattern confirmed across
  IMG_4495-IMG_4501. See IMG_4500.md for full discussion of the discrepancy with the existing
  IMG_4499.md (which recorded the reverse). Content: tail end of Function IsExternalWindowOpen
  (Else-branch closing, End Function), then a NEW function "Tree_IsNodeValid(ByVal pstrNodeKey)"
  (delegates to document.frames("tree").Tree_IsNodeValid), then the start of a NEW function
  "Tree_SelectNode(ByVal pstrNodeKey)". Both new functions follow the same
  Dim/On Error Resume Next/Set objDoc = document.frames("tree")/If Not objDoc Is Nothing
  Then .../TrapBrowserError pattern seen in SelectedNodeKey/PathDescription (IMG_4494) --
  i.e. this module is a frameset-controller delegating tree operations to a child frame named
  "tree", consistent with a top-level Main/Eebrowser-style frame-controller script.
---
```vbs
                Call TrapBrowserError("IsExternalWindowOpen|3")
                'Check if it is still open.
                If objWindow.closed Then
                    Call TrapBrowserError("IsExternalWindowOpen|4")
                    'Remove the erroneous entry.
                    mdicExternalWindows.Remove(CStr(pstrKey))
                    Call TrapBrowserError("IsExternalWindowOpen|5")
                Else
                    'window is open!
                    IsExternalWindowOpen = True
                End If
            End If
        End If
    End If
    Call TrapBrowserError("IsExternalWindowOpen|6")
    Set objWindow = Nothing
End Function
'------------------------------------------------------------------------------------------

Function Tree_IsNodeValid(ByVal pstrNodeKey)
'*****************************************************************************
' Returns True if the tree node with key pstrNodeKey is found
'*****************************************************************************
    Dim objDoc
    Tree_IsNodeValid = False

    On Error Resume Next

    Set objDoc = document.frames("tree")

    If Not objDoc Is Nothing Then
        Tree_IsNodeValid = objDoc.Tree_IsNodeValid(pstrNodeKey)
        Set objDoc = Nothing
    End If
    Call TrapBrowserError("Tree_IsNodeValid|1")

End Function
'------------------------------------------------------------------------------------------

Function Tree_SelectNode(ByVal pstrNodeKey)
'*****************************************************************************
    'Selects the tree node with key pstrNodeKey.
    'Returns true if the node is found.
```
