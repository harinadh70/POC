---
photo: IMG_4506.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible)
orientation: 0
confidence: high
notes: >
  MAJOR FINDING: This is NOT the StarTeam/DevOps archive viewer used for prior
  Eebrowser.vbs photos (IMG_4377-4433). This is a live Visual Studio 2022 IDE
  (solution "AQS.Advantage", startup project "AQS.Advantage.BatchEdit") with two
  open tabs: "Main_ISLLSYS_20010101.vbs" (active/foreground, gold-highlighted tab)
  and "Eebrowser.asp" (background tab, inactive). The code shown belongs to
  Main_ISLLSYS_20010101.vbs, a client/date-suffixed variant of a top-level
  "Main.vbs"-style frame-controller script (ISLLSYS = client code, 20010101 =
  date stamp in filename). A floating "Find in Files" search box (top right)
  shows the search term "Sub window_onbeforeunload" with scope "Entire solution",
  confirming the visible code is inside a Sub window_onbeforeunload routine in
  this file. Status bar shows editor cursor at "Ln: 2871 Ch: 26", implying this
  file is very large (well beyond Eebrowser.vbs's 1235 lines) -- consistent with
  a master/frame-controller script. No line-number gutter is visible in the
  editor (line numbers appear to be turned off), so exact line numbers for the
  visible code could not be determined. Note companion tab "Eebrowser.asp" --
  filename extension is .asp here, differing from the .vbs extension seen in
  the earlier StarTeam-viewer photos (Eebrowser.vbs); may be the same logical
  file under a different extension/location in this workspace, or a related
  wrapper file -- worth reconciling later.
---
```vb
Dim arrKeysNonPol
Dim intCountNonPolTemp


On Error Resume Next

blnOpenWindows = False

If StrComp(window.name, "v3Galileo", vbTextCompare) = 0 Then
    'This is the start page.
    'Check for open policy window.
    'If there are open policy windows, pass the policy window references to them.
    'This will allow any new search page to know which policies are open.
    If Not mdicChildWindows Is Nothing Then
        arrWindows = mdicChildWindows.Items
        For intCount = 0 To mdicChildWindows.Count - 1
            Set objWindow = arrWindows(intCount)
            If Not objWindow.closed Then
                'Make sure the correct page is still in the child window.
                Set objTemp = objWindow.document.all("fraMain")
                If Err.number = -2147024891 Then
                    Set objTemp = Nothing
                    Err.Clear
                End If
                If Not objTemp Is Nothing Then
                    'Policy windows still open, pass the references of child windows.
                    arrKeys = mdicChildWindows.Keys
                    arrWindowsTemp = mdicChildWindows.Items
                    'Set objWindow.mdicChildWindows = Nothing
                    For intCountTemp = 0 To mdicChildWindows.Count - 1
                        objWindow.LoadWindowReferences arrKeys(intCountTemp), arrWindowsTemp(intCountTemp)
                    Next
                    blnOpenWindows = True
                End If
            End If
        Next
    End If
    If Not mdicNonPolicyChildWindows Is Nothing Then
        arrNonPolWindows = mdicNonPolicyChildWindows.Items
        For intCount = 0 To mdicNonPolicyChildWindows.Count - 1
            Set objWindow = arrNonPolWindows(intCount)
            If Not objWindow.closed Then
                'Make sure the correct page is still in the child window.
```
