---
photo: IMG_4564.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers shown; editor status bar reads "Ln: 2871 Ch: 26")
orientation: 0
confidence: medium
notes: >
  Same Visual Studio session as IMG_4563: "Main_ISLLSYS_20010101.vbs" tab still
  shows active-tab styling (pin icon + X) at front of the tab strip, while
  "Eebrowser.asp" sits to its right with inactive styling, but status bar
  cursor is unchanged ("Ln: 2871 Ch: 26") and "Find "eebrowser.vbs"" panel
  still open bottom-left -- consistent with the hover/preview-tab hypothesis
  from IMG_4563 rather than an actual switch; filed as Eebrowser.asp. Content
  is a direct continuation of FillSelectList from IMG_4563 (first two lines
  duplicate IMG_4563's last two lines due to slight scroll overlap between
  photos). NOTE: several code lines are photographed at a slight camera skew
  that makes the tail of a long line (e.g. "Is Nothing Then", "Then") appear
  vertically aligned with the start of the NEXT source line rather than its
  own -- reconstructed each such line by rejoining the skewed tail to its
  syntactically-matching head (confirmed valid VBScript If/Then/Else/End If
  pairing in each case). This block closes out FillSelectList entirely: the
  "If blnAddOption Then" branch builds a new OPTION element (text from the
  listitem node, value from the "value" XML attribute or "" default, id from
  "covcode" attribute, selected=True + reset attribute back to "F" if
  selected="T"), adds it to objControl, closes the "For intItemCount" loop,
  calls TrapBrowserError("FillSelectList|1"), releases objTestNodes/objOption/
  objNamedNodeMapLI, and the final "End If" closes the outer
  "If objListItemsTest Is Nothing Then" opened all the way back in IMG_4562 --
  confirming that whole "add the rest of the list items" block only runs when
  there was no single matching listitemstest node. Ends with "End Sub".
---

```vb
                ' Not objNamedNodeMap.getNamedItem("multiselect") Is Nothing

            End If
                'Len(strRelatedList) > 0

            If blnAddOption Then
                Set objOption = objCallingWindow.document.createElement("OPTION")
                objOption.text = objListItems(intItemCount).text

                '--Get the "value" attribute of the listitem xml node--
                Set objNamedNodeMapLI = objListItems(intItemCount).attributes
                If Not objNamedNodeMapLI.getNamedItem("value") Is Nothing Then
                    objOption.value = objNamedNodeMapLI.getNamedItem("value").nodevalue
                Else
                    objOption.value = ""
                End If

                If Not objListItems(intItemCount).attributes.getNamedItem("covcode") Is Nothing Then
                    objOption.id = objListItems(intItemCount).attributes.getNamedItem("covcode").nodeValue
                End If

                '--See if the list item option should be selected--
                If Not objNamedNodeMapLI.getNamedItem("selected") Is Nothing Then
                    If objNamedNodeMapLI.getNamedItem("selected").nodevalue = "T" Then
                        objOption.selected = True
                        'Set to false. refresh list may then be done without initial items being re-selected.
                        objNamedNodeMapLI.getNamedItem("selected").nodevalue = "F"
                    End If
                End If
                '--Add option to the select control--
                objControl.add(objOption)
            End If
        Next
            'intItemCount = 0 To objListItems.length - 1

        Call TrapBrowserError("FillSelectList|1")

        Set objTestNodes = Nothing
        Set objOption = Nothing
        Set objNamedNodeMapLI = Nothing
    End If
End Sub
```
