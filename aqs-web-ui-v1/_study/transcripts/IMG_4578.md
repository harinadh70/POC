---
photo: IMG_4578.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers visible; editor cursor status bar shows "Ln: 2871, Ch: 26")
orientation: 0
confidence: medium
notes: >
  Viewer is Visual Studio (full IDE - Debug/Any CPU toolbar, Team/Test/Analyze menus,
  "AQS.Advantage.BatchEdit" project, "AQS.Advantage" solution in title bar), NOT the
  StarTeam/DevOps archive viewer used in the prior batch (IMG_4377-4433), and not VS Code.
  Active/focused tab reads "Eebrowser.asp" (note extension .asp, not .vbs as in the prior
  batch's "Eebrowser.vbs"). A second, pinned tab to its left is partially cut off at the
  photo edge, reading "...ain_ISLLSYS_20010101.vbs" -- likely "Main_ISLLSYS_20010101.vbs"
  or similar; POSSIBLE match for the Main/AQSMain frame-controller script being sought,
  but not confirmed since content shown belongs to the Eebrowser.asp tab and the other
  tab's contents were not visible in this photo. Content (Dim strItemXmlList, With
  objCallingWindow, TrapBrowserError("EEBrowser|GetXMLData|1"), mxmlDoc.selectnodes) is
  clearly part of the EEBrowser GetXMLData routine. Cursor position "Ln: 2871" is far
  beyond the previously-noted 1235-line total for Eebrowser.vbs, suggesting Eebrowser.asp
  is either a much longer/different file or line count differs substantially from the
  .vbs version -- worth flagging. No line-number gutter is visible in this editor, so the
  displayed line range could not be determined directly.
---

```vbs
        Dim strItemXmlList
        Dim strItemAction
        Dim strItemText
        Dim strItemEvent
        Dim strActionList
        Dim objActionList

        Dim strUsrOpt                                          ' Action List vars
        Dim objdtaTB3


                                        ' Object representing the Rate Tab on the Menu frame – used to determine
        Dim objMenu                    ' whether the rate button (if it exists) should be enabled or not.
        Dim intFrameCount
        Dim strFrameName
        '~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

        'Note: mxmlDoc is a module-level XML document that was set in the
        'window_onload event.
        '~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

        With objCallingWindow
            '--Find all the control nodes in the XMLDI--
            blnCutPaste = False
            Call UserOption("cutpaste", "T", strUsrOpt)
            If StrComp(strUsrOpt, "T", vbTextCompare) = 0 Then
                blnCutPaste = True
            End If

            If Len(pstrFilter) > 0 Then
                strFilter = pstrFilter
            End If

            Set objNodes = .mxmlDoc.selectnodes("//control" & strFilter)
                Call TrapBrowserError("EEBrowser|GetXMLData|1")

            '--Spin through the returned nodes--
            For intCount = 0 To objNodes.length - 1

                blnLimitToList = True

                    'set/reset this flag (true is the default)

                '--Get the matchcode of the current xml node--
                Set objNamedNodeMap = objNodes(intCount).attributes
```
