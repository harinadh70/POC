---
photo: IMG_4654.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26 -- unchanged from prior photos in this run)
orientation: 0
confidence: medium
notes: >
  Same VS session/tabs as IMG_4650-4653. Finishes the "SELECT" case's Case "X"
  ShowZero-based selectedIndex logic, closes "End Select 'strVarType", then Case "DIV"
  (clears innerText, has a dedup guard against "*" markers next to a "</span>" before
  setting innerHTML), then Case "TEXTAREA" (objControl.value = vntValue). The whole
  dispatcher then closes with "End Select 'UCase(objControl.tagname)" -- CONFIRMS the
  outer dispatch is keyed on UCase(objControl.tagname), i.e. this is a single big
  SetControlValue-style routine dispatching on HTML tag name (CHECKBOX/RADIOBUTTON/
  XMLLIST/COMBO/KPCOMBO/IBUTTON/INPUT/SELECT/DIV/TEXTAREA), confirming the earlier
  "duplicate CHECKBOX" was just this outer tagname-dispatch vs. the inner
  UCase(objControl.type) dispatch nested under "Case INPUT". Immediately after, new
  comment/code begins about a control losing focus while its value was changed by a
  "browser command", referencing mstrPreviousMatchcode and objControl.name compared via
  StrComp, "used by the onfocushandler" -- this looks like the tail of the current sub
  transitioning into a focus-handling routine.
---

```vbs
            Case "X"
                If objControl.ShowZero = "T" Then
                    'We can simply take the index value and use as-is since there

                    'is a "real" zero element.
                    objControl.selectedIndex = vntValue
                        'Sets the "X" variable
                Else
                    'objControl.ShowZero = "F"
                    'Since there is NO "real" zero element, we need to shift the
                    'index value down one to match the element in the combobox.
                    objControl.selectedIndex = vntValue - 1
                        'Sets the "X" variable
                End If

        End Select
                'strVarType

    Case "DIV"
        objControl.innerText = ""

        '--prevent *s from being added multiple times
        If instr(1,vntValue, "*") > 0 AND instr(1,vntValue, "span") > 0 Then
            vntValue = replace(vntValue, "</span>*", "</span>")
        End If
        objControl.innerHTML = vntValue

    Case "TEXTAREA"
        objControl.value = vntValue
            'Sets the "L" variable

End Select
    'UCase(objControl.tagname)

'If the control that just lost focus is having its value changed by a browser command,
'need to reset the inital value indicator to prevent post processing.
If StrComp(mstrPreviousMatchcode, objControl.name, vbTextCompare) = 0 Then
    'this is used by the onfocushandler
```
