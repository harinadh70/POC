---
photo: IMG_4451.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (line numbers not shown in editor gutter)
orientation: 0
confidence: high
notes: |
  Same Visual Studio session as prior photos ("Main_ISLLSYS_20010101.vbs" active/gold, "Eebrowser.asp"
  inactive). Finishes Function LogoutGrandParent() (mdicNonPolicyChildWindows cleanup block, then
  objGrandParentWindow.close, then 4 stacked End If closing back out to the top of the function, then
  Call TrapBrowserError("LogoutGrandParent|5") / End Function). NOTABLE: the TrapBrowserError call-number
  sequence jumps from "LogoutGrandParent|3" straight to "LogoutGrandParent|5" -- "|4" is never called
  anywhere in this function, i.e. a numbering gap in the source (verified via high-zoom crop, not a
  transcription error). ALSO NOTABLE: in the mdicNonPolicyChildWindows sub-block here (and previously in
  Function Logout() in IMG_4449) the per-window guard reads "If Not objWindow.close Then" (calling/reading
  the .close METHOD as if it were a boolean), whereas the mdicExternalWindows/mdicChildWindows sub-blocks
  in this same LogoutGrandParent() function (IMG_4450) correctly use "If Not objWindow.closed Then" (the
  boolean .closed PROPERTY) -- a probable copy/paste bug in the original source, transcribed verbatim as
  shown; confirmed via high-zoom crop of the exact word ("close" not "closed"). After End Function, a new
  function begins: Function ShowBatchEdits (ByVal pstrType) -- "This function looks at mxmlBatchEdits for
  any data. If there is xml, it launches a window that will display the information." This references a
  page-level XML data island variable mxmlBatchEdits, distinct from the window-management machinery seen
  so far. No line-number gutter enabled.
---
```vbs
                    If objGrandParentWindow.mdicNonPolicyChildWindows.Count > 0 Then
                        arrWindows = objGrandParentWindow.mdicNonPolicyChildWindows.Items
                        For intCount = 0 To objGrandParentWindow.mdicNonPolicyChildWindows.Count - 1
                            Set objWindow = arrWindows(intCount)
                            If Not objWindow Is Nothing Then
                                If Not objWindow.close Then
                                    objWindow.close
                                End If
                            End If
                        Next
                    End If
                    Call TrapBrowserError("LogoutGrandParent|3")
                End If
                objGrandParentWindow.close
            End If
        End If
    End If
End If
Call TrapBrowserError("LogoutGrandParent|5")
End Function
'--------------------------------------------------------------------------


Function ShowBatchEdits (ByVal pstrType)
'****************************************************************************
'This function looks at mxmlBatchEdits for any data.
'If there is xml, it launches a window that will display the information.
'****************************************************************************
    Dim objWindow


    Dim intTop
    Dim intLeft
    Dim strWindowOptions
    Dim strURL

    On Error Resume Next
    ShowBatchEdits = False

        If Not mxmlBatchEdits Is Nothing Then
            If Len(mxmlBatchEdits.xml) > 0 Then
                ShowBatchEdits = True
                intTop = window.screenTop
                intLeft = window.screen.width - 350
```
