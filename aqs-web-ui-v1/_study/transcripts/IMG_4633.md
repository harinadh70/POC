---
photo: IMG_4633.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible in editor; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4632, same tab "Main_ISLLSYS_20010101.vbs"
  (Eebrowser.asp inactive tab still present). Body of Sub SetControlAttribute
  begins: On Error Resume Next, Dim objRelatedControl (comment: handle to a
  related control like a calendar button), Dim blnBooleanAttribute, Dim
  strOrigValue, Dim objMenu, Dim strUsrOpt. Then a block "Get disabled color,
  first time only": If IsEmpty(mintDisabledBlue) Then Set objMenu =
  window.frames("menu"), reads mintDisabledRed/Green/Blue by splitting
  objMenu.mColorCtlDisabledDec on "|"; checks
  mobjAqsMain.UserOption("disfldbackgroundcolor","T",strUsrOpt) to pick
  mhexDisabledColor from either mColorCtlDisabledHexYellow or
  mColorCtlDisabledHex; sets several more mhex*/mint* module-level color
  variables from objMenu.mColorCtl* properties (DisabledLabelHex,
  RequiredLabelHex, ComboLTLFalseHex, ComboLTLTrueHex [note: source property
  name is misspelled "mColorCltComboLTLTrueHex", Clt not Ctl -- verbatim],
  RequiredBackgroundHex/Dec), then Set objMenu = Nothing, End If. Finally
  begins a second "Get LTL highlighting setting, first time only" block: If
  IsEmpty(mblnEnableLTLHighlight) Then If UserOption("ltlhighlight","T",
  strUsrOpt) Then 'Recheck for value (default needs to be disabled). IMPORTANT:
  introduces the object reference "mobjAqsMain" (module-level object, used as
  mobjAqsMain.UserOption(...)) -- a strong naming link to "AqsMain" /
  "AQSMain", though here it is a variable/object reference inside
  Main_ISLLSYS_20010101.vbs, not a separate file named Main.vbs/AQSMain.vbs.
  Also uses a bare (non-prefixed) "UserOption(...)" call later, distinct from
  "mobjAqsMain.UserOption(...)" used earlier in this same block -- flagged as
  notable, not normalized in transcription.
---
```vbs
On Error Resume Next

'A handle to a related control, Like a calendar button with
'a date control, so we can apply the same attributes to it.
Dim objRelatedControl

Dim blnBooleanAttribute                    'Flag indicating the attribute is a boolean type.

Dim strOrigValue                           'The original value - not converted into a boolean value.
Dim objMenu
Dim strUsrOpt

'-- Get disabled color, first time only
If IsEmpty(mintDisabledBlue) Then
    Set objMenu = window.frames("menu")
    mintDisabledRed = Split(objMenu.mColorCtlDisabledDec, "|")(0)
    mintDisabledGreen = Split(objMenu.mColorCtlDisabledDec, "|")(1)
    mintDisabledBlue = Split(objMenu.mColorCtlDisabledDec, "|")(2)

    'if other background colors is needed for disabled fields.
    If mobjAqsMain.UserOption("disfldbackgroundcolor", "T", strUsrOpt) Then
        If StrComp(strUsrOpt, "T", vbTextCompare) = 0 Then
            mhexDisabledColor = objMenu.mColorCtlDisabledHexYellow
        Else
            mhexDisabledColor = objMenu.mColorCtlDisabledHex
        End if
    End If

    mhexDisabledLabelColor = objMenu.mColorCtlDisabledLabelHex
    mhexRequiredLabelColor = objMenu.mColorCtlRequiredLabelHex
    mhexComboLTLFalseColor = objMenu.mColorCtlComboLTLFalseHex
    mhexComboLTLTrueColor = objMenu.mColorCltComboLTLTrueHex
    mhexRequiredBackgroundColor = objMenu.mColorCtlRequiredBackgroundHex
    mintRequiredBackgroundRed = Split(objMenu.mColorCtlRequiredBackgroundDec, "|")(0)
    mintRequiredBackgroundGreen = Split(objMenu.mColorCtlRequiredBackgroundDec, "|")(1)
    mintRequiredBackgroundBlue = Split(objMenu.mColorCtlRequiredBackgroundDec, "|")(2)
    Set objMenu = Nothing
End If

'-- Get LTL highlighting setting, first time only
If IsEmpty(mblnEnableLTLHighlight) Then
    If UserOption("ltlhighlight", "T", strUsrOpt) Then
        'Recheck for value (default needs to be disabled)
```
