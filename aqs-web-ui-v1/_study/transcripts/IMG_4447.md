---
photo: IMG_4447.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (line numbers not shown in editor gutter)
orientation: 0
confidence: high
notes: |
  Same Visual Studio session/tabs as IMG_4446 ("Main_ISLLSYS_20010101.vbs" active/gold-highlighted,
  pinned; "Eebrowser.asp" inactive/dark-blue in background). Confirms file identity: this photo shows
  Function LogoutMain(), logically continuing shortly after AddWindowToCollection from IMG_4446 (same
  file, "windows cleanup" section of Main_ISLLSYS_20010101.vbs). This strongly confirms
  Main_ISLLSYS_20010101.vbs is a top-level/frame-controller-style script -- it owns application logout
  and enumerates three distinct window-tracking dictionaries: mdicExternalWindows, mdicChildWindows, and
  (cut off at bottom of frame) mdicNonPolicyChildWindows, closing every open window in each before/while
  logging the user out via TrapBrowserError("LogoutMain|1") / ("LogoutMain|2") calls. No line-number
  gutter enabled. Last line at bottom of the visible frame is likely truncated mid-block (file continues
  past what's shown into the mdicNonPolicyChildWindows cleanup loop).
---
```vbs
Function LogoutMain()
'****************************************************************************
'This function logs out the user from AQS when Logout is clicked on
'Home Page
'****************************************************************************
    Dim arrWindowsMain
    Dim objWindowLogoutMain

    If Not mdicExternalWindows Is Nothing Then
        If mdicExternalWindows.Count > 0 Then
            arrWindowsMain = mdicExternalWindows.Items
            For intCount = 0 To mdicExternalWindows.Count - 1
                Set objWindowLogoutMain = arrWindowsMain(intCount)
                If Not objWindowLogoutMain Is Nothing Then
                    If Not objWindowLogoutMain.closed Then
                        objWindowLogoutMain.close
                    End If
                End If
            Next
            Call TrapBrowserError("LogoutMain|1")
        End If
    End If

    If Not mdicChildWindows Is Nothing Then
        If mdicChildWindows.Count > 0 Then
            arrWindowsMain = mdicChildWindows.Items
            For intCount = 0 To mdicChildWindows.Count - 1
                Set objWindowLogoutMain = arrWindowsMain(intCount)
                If Not objWindowLogoutMain Is Nothing Then
                    If Not objWindowLogoutMain.closed Then
                        objWindowLogoutMain.close
                    End If
                End If
            Next
            Call TrapBrowserError("LogoutMain|2")
        End If
    End If
    If Not mdicNonPolicyChildWindows Is Nothing Then
        If mdicNonPolicyChildWindows.Count > 0 Then
            arrWindowsMain = mdicNonPolicyChildWindows.Items
            For intCount = 0 To mdicNonPolicyChildWindows.Count - 1
                Set objWindowLogoutMain = arrWindowsMain(intCount)
```
