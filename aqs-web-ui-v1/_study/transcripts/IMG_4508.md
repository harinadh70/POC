---
photo: IMG_4508.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4506/4507, same Visual Studio session/tabs. This photo
  captures the END of Sub window_onbeforeunload (ends with
  "Call TrapBrowserError(""window_onbeforeunload|1"")" then "End Sub"), a
  dashed comment-line section divider ('----...), and the START of the NEXT
  sub, "Sub window_onunload", with its banner comment block ("Close child
  windows if this is a policy window. / Then set focus to the parent window.")
  and variable declarations. This confirms Main_ISLLSYS_20010101.vbs is a
  frame-controller script implementing the window lifecycle handlers
  (window_onload / window_onbeforeunload / window_onunload) -- i.e. exactly the
  kind of top-level "Main.vbs"-style script the task is looking for. The
  TrapBrowserError("window_onbeforeunload|1") call pattern matches an
  error-trapping convention likely reused across every named Sub in this file.
---
```vb
            If Not (objScript is Nothing) Then
                Set objScript = Nothing
                blnEEDataChanged = objDoc.mblnEEDataChanged
            If Not objDoc.mxmlDoc.selectSingleNode("//page[@ignorechanges=""T""]") Is Nothing Then
                blnIgnoreChanges = True
            End If
            Set objDoc = Nothing
                End If
            End If

        If (mblnDataChanged Or blnEEDataChanged) And Not blnIgnoreChanges And Not mblnLogout Then
            If strcomp(mstrAction, "MSG", vbTextCompare) <> 0 Then
                window.event.returnValue = "This page is being closed before completion of the current operation.  Data may be lost if you continue."
            End If
        End If
        'Refresh the WIP in the event any items shown there have changed.
        Call RefreshSearchPageLists()
    End If
    Call TrapBrowserError("window_onbeforeunload|1")
End Sub
'--------------------------------------------------------------------------------------

Sub window_onunload
'****************************************************************************
' Close child windows if this is a policy window.
' Then set focus to the parent window.
'****************************************************************************

    Dim arrWindows
    Dim intCount
    Dim objWindow
    Dim arrKeys
    Dim strWindowName
    Dim objDoc
    Dim objScript

    On Error Resume Next
    'Close the child windows of this policy
    'do not close other policy windows if the a replacement search window was needed.

    If Not mdicChildWindows Is Nothing Then
        arrWindows = mdicChildWindows.Items
        arrKeys = mdicChildWindows.Keys
```
