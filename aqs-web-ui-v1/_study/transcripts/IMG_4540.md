---
photo: IMG_4540.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter shown; editor status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4539 (same Visual Studio window, same active tab
  "Main_ISLLSYS_20010101.vbs", same inactive background tab "Eebrowser.asp", same status-bar
  cursor position Ln 2871/Ch 26). Finishes the "SELECT" sub-case body inside LOAD_COMBO/LOAD_COMBO2
  (objOption.text/.value from GetStringField(.marrListItems(intCount),...,"|"), objTempControl.add),
  closes the tagName Select Case and the outer If Not objTempControl Is Nothing block. Then two new
  top-level Case blocks: "LOAD_XMLDOCUMENT" (Execute strNoun & ".loadXML " ... loads an XML string
  into a data island via dynamic Execute, after stripping quotes/CR/LF) and "LOAD_XMLLIST" (same
  quote/CR/LF stripping, then Execute strNoun & ".xmlData = " ... assigns xml string to an xmllist
  object). Then a new Case "OPEN_WINDOW" begins (window.open call), continuing onto IMG_4541.
  Both TrapBrowserError calls use the consistent "EEBrowser|ExecuteBrowserCommands|<CASE>|1" prefix
  format seen in most other cases in this batch. No Main.vbs/AQSMain.vbs filename appears in this
  photo.
---
```vbscript
                objOption.text = GetStringField(.marrListItems(intCount),1,"|")
                objOption.value = GetStringField(.marrListItems(intCount),2,"|")
                objTempControl.add ( objOption )
            Next

        End Select
            ' UCase(objTempControl.tagName)
    End If
        'Not objTempControl Is Nothing
    '===================================================

Case "LOAD_XMLDOCUMENT"
    '=====Put xml into data island on calling page
    ' strNoun data island name
    ' strAddInf xml string

    strAddInf = Replace(strAddInf,Chr(34),"'")
    strAddInf = Replace(strAddInf,Chr(10),"")
    strAddInf = Replace(strAddInf,Chr(12),"")

    Execute strNoun & ".loadXML " & Chr(34) & strAddInf & Chr(34)

        Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|LOAD_XMLDOCUMENT|1")
    '===================================================

Case "LOAD_XMLLIST"

    ' strNoun   xmllist name (include . in front if list on calling window
    ' strAddInf xml string

    strAddInf = Replace(strAddInf,Chr(34),"'")
    strAddInf = Replace(strAddInf,Chr(10),"")

    strAddInf = Replace(strAddInf,Chr(12),"")

    Execute strNoun & ".xmlData = " & Chr(34) & strAddInf & Chr(34)

    Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|LOAD_XMLLIST|1")
    '===================================================
Case "OPEN_WINDOW"

    window.open strAddInf, strNoun, "channelmode=no,directories=no,fullscreen=no,height=550,location=no,menubar=no," & _
```
