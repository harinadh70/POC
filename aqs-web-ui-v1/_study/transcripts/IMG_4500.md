---
photo: IMG_4500.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; status bar shows Ln 1405, Ch cut off; zoom "79%")
orientation: 0
confidence: high
notes: >
  CORRECTION vs. prior batch's IMG_4499.md: at high zoom on the tab strip, "Main_ISLLSYS_20010101.vbs"
  is the ACTIVE tab here (tan/pinned background, bold black text, no extra highlight box) while
  "Eebrowser.asp" is a plain inactive tab (dark navy background, thin light text). IMG_4499.md
  (already on disk, not modified per instructions) recorded the opposite -- "Eebrowser.asp active
  tab; Main_ISLLSYS_20010101.vbs inactive tab still unopened" -- for what is pixel-for-pixel the
  same visible code (same Function IsExternalWindowOpen body, same Ln:1405 cursor position, same
  "79%" zoom, same bottom-panel tabs incl. Find "eebrowser.vbs"). This photo appears to be a
  re-take/duplicate of the same screen state as IMG_4499, but the tab-highlight evidence here
  indicates the code on screen (in this and likely the preceding several photos back to at least
  IMG_4495) is being viewed/edited in Main_ISLLSYS_20010101.vbs, not Eebrowser.asp. Re-checked
  IMG_4495/4496/4497 tab crops directly: all three show the same tan+bold Main_ISLLSYS_20010101.vbs
  / plain-navy Eebrowser.asp pattern as this photo. Only IMG_4494 showed the reverse (Eebrowser.asp
  tab with a distinct blue highlight box). This strongly suggests Main_ISLLSYS_20010101.vbs is a
  genuine Main*.vbs-style file (per-LOB "Main" script naming convention: Main_<LOB>_<date>.vbs)
  that contains this same OpenNewWindow/BlankModelessWindowSetFocus/mblnPolicyOpen/
  IsExternalWindowOpen window-management code -- either because it IS the file being edited, or
  because Main_ISLLSYS_20010101.vbs duplicates/shares this boilerplate with Eebrowser.asp. Flagging
  for parent task resolution; see also IMG_4501-4505 which continue to show Main_ISLLSYS_20010101.vbs
  as the bold/active tab.
---
```vbs
    On Error Resume Next
    'Default - Assume window is not open
    IsExternalWindowOpen = False

    If StrComp(Left(window.name,6), "policy", vbTextCompare) = 0 Then
        'Create the dictionary it not already done.
        If mobjParentWindow.mdicExternalWindows Is Nothing Then
            Set mobjParentWindow.mdicExternalWindows = CreateObject("Scripting.Dictionary")
            Call TrapBrowserError("IsExternalWindowOpen|1")
        End If
        If mobjParentWindow.mdicExternalWindows.count > 0 Then
            If mobjParentWindow.mdicExternalWindows.Exists(CStr(pstrKey)) Then
                Call TrapBrowserError("IsExternalWindowOpen|2")
                'The window has been opened already...
                Set objWindow = mobjParentWindow.mdicExternalWindows.Item(CStr(pstrKey))
                Call TrapBrowserError("IsExternalWindowOpen|3")
                'Check if it is still open.
                If objWindow.closed Then
                    Call TrapBrowserError("IsExternalWindowOpen|4")
                    'Remove the erroneous entry.
                    mobjParentWindow.mdicExternalWindows.Remove(CStr(pstrKey))
                    Call TrapBrowserError("IsExternalWindowOpen|5")
                Else
                    'window is open!
                    IsExternalWindowOpen = True
                End If
            End If
        End If
    Else
        'Create the dictionary it not already done.
        If mdicExternalWindows Is Nothing Then
            Set mdicExternalWindows = CreateObject("Scripting.Dictionary")
            Call TrapBrowserError("IsExternalWindowOpen|1")
        End If
        If mdicExternalWindows.count > 0 Then
            If mdicExternalWindows.Exists(CStr(pstrKey)) Then
                Call TrapBrowserError("IsExternalWindowOpen|2")
                'The window has been opened already...
                Set objWindow = mdicExternalWindows.Item(CStr(pstrKey))
                Call TrapBrowserError("IsExternalWindowOpen|3")
                'Check if it is still open.
                If objWindow.closed Then
                    Call TrapBrowserError("IsExternalWindowOpen|4")
```
