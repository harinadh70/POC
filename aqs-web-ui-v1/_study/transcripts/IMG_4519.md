---
photo: IMG_4519.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter line numbers shown; editor cursor position reads Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Same Visual Studio session as IMG_4518 (active tab Main_ISLLSYS_20010101.vbs, inactive
  Eebrowser.asp tab). Continues directly from IMG_4518: closes out the nested If/End If chain,
  then "Call TrapBrowserError("ApplySecurityPolicy|8")" and "End Sub" -- confirming this whole
  block (IMG_4518+IMG_4519) is a single Sub, apparently named for/around "ApplySecurityPolicy"
  (TrapBrowserError checkpoint strings "ApplySecurityPolicy|7" then "|8" bracket it). A horizontal
  comment-dash divider follows, then a NEW named sub begins: "Sub ButtonOnClickHandler (ByRef
  objCallingWindow)" with a full purpose header comment block describing it as the OnClick
  handler for various buttons, relating clicked buttons to XML page data via a "relatedcontrol"
  attribute, appending to an "EEData" array for post-processing via ".CallServer", with special
  handling when the related control is "XMLLIST". Variable declarations and beginning of body
  follow (On Error Resume Next; blnCallServer/blnCheckRating flags; With objCallingWindow /
  Set objControl = .event.srcElement; comment block on related-control logic; first
  .mxmlDoc.selectSingleNode(...) call using @matchcode/@relatedcontrol XPath). This confirms
  Main_ISLLSYS_20010101.vbs is a large shared script file containing many named Subs (a
  frame/utility-controller style file), distinct from Eebrowser.vbs.
---
```vbs
            If StrComp(strDisabled, "T", vbTextCompare) = 0 Then
                Call objControl.setAttribute ("SecDis", "T")
            End If
        End If
    End If
End If
Call TrapBrowserError("ApplySecurityPolicy|8")
End Sub
'------------------------------------------------------------------------------------------------

Sub ButtonOnClickHandler (ByRef objCallingWindow)
    '**************************************************************************************
    ' PURPOSE:
    ' This subroutine is the OnClick handler for various buttons.
    ' If the button has a related control definded in the xml page data,
    ' then the value/data from that control is added to the EEData array
    ' for post processing. ( Call .CallServer )
    ' In the case where the related control is XMLLIST, the special
    ' handler for XMLLIST is called.
    '
    '**************************************************************************************

Dim objControl
Dim objRelatedControl
Dim objNode
Dim blnCallServer                          'Used to prevent server call in special cases

Dim strTemp
Dim blnCheckRating                         'Used to identify controls that affect rating

    On Error Resume Next

    blnCallServer = True
    blnCheckRating = True

    With objCallingWindow
        Set objControl = .event.srcElement

        ' Determine if the button clicked has a related control...
        ' If it does, this is the item that holds the data/value (to post process/ save)
        ' Otherwise, simply post process the button
        Set objNode = .mxmlDoc.selectSingleNode("//control [@matchcode = """ & objControl.name & """ and @relatedcontrol]")
```
