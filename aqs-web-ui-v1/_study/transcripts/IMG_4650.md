---
photo: IMG_4650.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: medium
notes: >
  This is NOT the StarTeam/DevOps archive viewer used for prior photos -- it is live
  Visual Studio 2022 (solution "AQS.Advantage", startup project "AQS.Advantage.BatchEdit",
  GitHub Copilot Chat panel visible). Two tabs are open: "Main_ISLLSYS_20010101.vbs"
  (gold/tan tab with pin icon, leftmost) and "Eebrowser.asp" (blue tab, note .asp not
  .vbs extension). The status bar shows Ln:2871, which cannot fit inside the
  previously-catalogued 1235-line Eebrowser.vbs, so the visible code most likely
  belongs to Main_ISLLSYS_20010101.vbs -- a NEW filename matching the "Main.vbs"-style
  frame-controller pattern the task is watching for. However tab-focus is ambiguous
  from the photo (cannot be 100% certain which of the two open tabs is the active
  one), so file attribution confidence is medium. The code itself is a SetControlValue-
  style dispatcher (Select Case on control type/strVarType: checkbox "L"/"X" cases,
  RADIOBUTTON, XMLLIST with a comment about a transformation-triggered reload and an
  internal one-time-execution flag, COMBO/KPCOMBO beginning). No line numbers are
  shown in a gutter in this VS view, unlike the archive viewer used previously.
---

```vbs
        Select Case strVarType
            Case "L"

                If vntValue = "YES" Then
                    objControl.checked = True
                        'Set based on the "L" variable
                Else
                    'vntValue = "NO"
                    objControl.checked = False
                        'Set based on the "L" variable
                End If

            Case "X"
                If vntValue = 1 Then
                    objControl.checked = True
                        'Set based on the "X" variable
                Else
                    objControl.checked = false
                        'Set based on the "X" variable
                End If
        End Select

    Case "RADIOBUTTON"
        Select Case strVarType
            Case "L"
                objControl.value = vntValue
                    'Sets the "L" variable
            Case "X"
                objControl.selectedindex = vntValue
                    'Sets the "X" variable
        End Select
            'strVarType

    Case "XMLLIST"
        'This method only has an affect if the XMLLIST behavior
        'is being loaded as the result of a transformation after
        'the page has been loaded.  Otherwise, the code would have been
        'executed when the window loaded.  An internal flag prevents
        'the code from executing more than once.

        objControl.Reset

    Case "COMBO", "KPCOMBO"
        Select Case strVarType
```
