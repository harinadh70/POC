---
photo: IMG_4554.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers shown; editor cursor status bar reads "Ln: 2871")
orientation: 0
confidence: high
notes: >
  IMPORTANT DEPARTURE FROM PRIOR BATCH: this is NOT the StarTeam/DevOps archive
  viewer used for IMG_4377-4433. It is a live Visual Studio IDE (menu bar reads
  File Edit View Git Project Build Debug Team Test Analyze Tools Extensions
  Window Help; solution/project selector top-right reads "AQS.Advantage" /
  "AQS.Advantage.BatchEdit"; Debug/Any CPU config dropdowns visible). Two tabs
  are open: the active/focused tab is "Eebrowser.asp" (note .asp extension, NOT
  .vbs as previously transcribed), and a second, non-focused tab whose name is
  truncated by the photo's left edge, reading "...ain_ISLLSYS_20010101.vbs" --
  most likely "Main_ISLLSYS_20010101.vbs" (could also be a longer name ending
  in "...ain_ISLLSYS_20010101.vbs", e.g. "Domain_..."). This is a strong
  candidate for the top-level Main.vbs-style frame-controller file the task is
  hunting for -- flagging for a follow-up photo/zoom if available. The status
  bar shows cursor at "Ln: 2871" with NO line-number gutter displayed in the
  editor itself, so exact line numbers for the visible code below cannot be
  read off the image; the code shown is presumably in the general vicinity of
  line 2871 but this is not confirmed. This Eebrowser.asp file being 2800+
  lines is far larger than the 1235-line Eebrowser.vbs referenced in the task
  description, suggesting Eebrowser.asp is a different/expanded file sharing
  only the base name, not literally a continuation past line 438 of the .vbs
  file. Content shown is inside a Select Case block (case values SET_VISIBILITY
  [tail end], SHOW_EDITS, SHOW_PROGRESS) that appears to be the body of an
  "ExecuteBrowserCommands" routine on an "EEBrowser" object, per the
  TrapBrowserError log strings ("EEBrowser|ExecuteBrowserCommands|...").
---

```vb
Call GetSecurityValues(strProgramType,strPageMatchCode,strNoun,blnSecDisabled,blnSecVisible)
End With
'// End add for functional security

Set objTempControl = objDoc.document.all("dta" & strNoun)
Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|SET_VISIBILITY|1")
If blnSecVisible Then
    Call SetControlAttribute(objTempControl, strNoun, "VISIBLE", UCase(strAddInf), objDoc)
End If
'==========================================
              Case "SHOW_EDITS"
Set mxmlBatchEdits = CreateObject("MSXML2.DOMDocument.6.0")
With mxmlBatchEdits
  .async = false
  .LoadXML strAddInf
End With
Call ShowBatchEdits(strNoun)
Call TrapBrowserError("ExecuteBrowserCommands|SHOW_EDITS")
Case "SHOW_PROGRESS"

              '=====Open a modal dialog window which starts an async process, then returns xml doc===========
              'Open the ShowProgress.asp page in a modal dialog window with the querystring as built below.
              'Include the array element strAddInf for additional query string values.

              'strAddInf  should contain:

              '   &object      = [ name in case statement in XmlServercall.aspx]
              '   &function    = [ text to identify process to user and timing label]

              'Clear previous return values
              Set .mxmlResults = Nothing
                  Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|SHOW_PROGRESS|1.1")

              'Set up querystring
              strQS = "?userid="                    & .marrSessionInformation(1) & _
                        "&policyid="                  & .marrSessionInformation(2) & _
                        "&nodekey="                   & .marrSessionInformation(3) & _
                        "&action="                    & .marrSessionInformation(4) & _
                        "&diagnosticmode="  & .marrSessionInformation(5) & _
                        "&xmldetail="                 & Escape(.marrSessionInformation(6)) & _
                        "&modal=1"                    & _
                        "&returntype=xml"      & _
                        strAddInf
                  Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|SHOW_PROGRESS|1.2")
```
