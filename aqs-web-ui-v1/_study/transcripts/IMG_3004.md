---
photo: IMG_3004.JPG
type: vscode-code
file: aqs-web-ui/src/pages/PolicyInformationPage.tsx
lines: 11-44
orientation: 180
confidence: medium
notes: >
  Same file as IMG_3003 (PolicyInformationPage.tsx), scrolled down; overlaps IMG_3003 for
  lines 11-34 and extends to the end of the file at line 44 (closing `);`). The photo has a
  double-exposure / motion-blur ghosting artifact affecting roughly lines 17-40 (two
  overlapping renders of the same content, offset vertically by ~7-9 lines, similar to the
  artifact seen in IMG_3002) — lines 11-16 and 41-44 are sharp/single and read with high
  confidence; the ghosted middle section content was reconstructed by combining both
  overlapping layers (which agree with each other and with IMG_3003 on lines 11-34) plus
  structural JSX-nesting logic for the new lines 35-44.
  Sticky scroll shows line 11: "export default function PolicyInformationPage() {".
  Tab bar: "PolicyInformationPage.tsx 9+", breadcrumb
  aqs-web-ui > src > pages > PolicyInformationPage.tsx > ...
  Explorer sidebar: same as IMG_3003 (PolicyInformationPage.tsx highlighted, 9+).
  Workspace: AQS_workspace, branch hitanshu/experimental*. Status bar: 18 errors, 0 warnings,
  "No Solution". Top-right corner shows the same small red/orange graphic artifact seen in
  IMG_3003 (unclear content, likely a reflection or notification thumbnail, not code).
  This appears to be the end of the file: line 44 is `);` closing the return statement; the
  final closing `}` of the function (line 45) is not visible (cut off at the bottom of the
  photo by the status bar/taskbar).
  Laptop keyboard visible framing top of photo (same physical setup as IMG_3003).
  Cursor shown as Ln 1, Col 1 in status bar (stale/inconsistent with visible content).
---
Sticky scroll (line 11):
```
11      export default function PolicyInformationPage() {
```

Main editor (lines 12-44):
```
12          const loaderData = useLoaderData() as any;
13          const [loading] = useState(false);
14
15          const policyId = getItem<Record<string, unknown>>('aqs:global-variables', {})?.mstrPolicyID;
16
17          if (loading)
18              return (
19                  <Suspense fallback={<Loader />}>
20                      <main className="p-4 md:p-6">
21                          <Loader />
22                      </main>
23                  </Suspense>
24              );
25
26          if (isEmpty(loaderData?.pageBuild)) return null;
27
28          return (
29              <TabContextProvider>
30                  <div className="grid grid-cols-4 gap-x-2 items-start w-full h-full">
31                      <div className="col-span-1 py-4! px-8! relative h-full bg-[#F9F7F0] min-h-216 ">
32                          <h1 className="text-[16px] font-semibold text-left text-[#00205B] ">
33                              Policy Structure
34                          </h1>
35                          <Typography className=" policy-id-no ">
36                              Policy {(policyId || '') as string}
37                          </Typography>
38                      </div>
39                      <div className="col-span-3 px-8! p-4! customGridWrapper">
40                          <PolicyInformation />
41                      </div>
42                  </div>
43              </TabContextProvider>
44          );
```
Note: line 12's own row in this photo is faint/ghosted (overlapped with a bleed-through of line
15's `getItem<Record<string, unknown>>('aqs:global-variables', {})?.mstrPolicyID` expression);
the text shown above for line 12 is taken from IMG_3003's clean, high-confidence transcription
of the same line rather than this photo's ghosted rendering.
