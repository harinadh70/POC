---
photo: IMG_4573.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4572, same VS window/tabs (Main_ISLLSYS_20010101.vbs inactive, Eebrowser.asp active). Continues
  GetControlValue: finishes Case "ICHECKBOX" (Else vntValue="NO"), then Case "XMLLIST" (.action), Case
  "COMBO","KPCOMBO" (.text), Case "INPUT" with nested Select Case UCase(objControl.type) covering
  TEXT/PASSWORD/BUTTON/SUBMIT/HIDDEN/FILE (.value), CHECKBOX (.checked -> YES/NO), and RADIO (noted as unused, with
  comment "NOTE: We are not using this control and instead using the RADIOBUTTON behavior control", author-dated
  "(tww 10/17/2000)" appears later on the SELECT case comment). Then Case "SELECT" begins with a comment about
  selectedIndex assumptions, cut off at `If objControl.selectedIndex <> -1 Then`. Author initials "tww" and date
  10/17/2000 visible in a code comment -- useful for dating this revision of the file. No AQSMain/Main.vbs content
  visible. No line-number gutter.
---
```vbscript
        Case "ICHECKBOX"
            If objControl.checked Then
                vntValue = "YES"
                    'Returns the "L" variable
            Else
                vntValue = "NO"
                    'Returns the "L" variable
            End If

        Case "XMLLIST"
            vntValue = objControl.action
                'Returns the "L" variable

        Case "COMBO", "KPCOMBO"
            vntValue = objControl.text
                'Returns the "L" variable

        Case "INPUT"
            Select Case UCase(objControl.type)
                Case "TEXT","PASSWORD","BUTTON","SUBMIT", "HIDDEN","FILE"
                    vntValue = objControl.value
                        'Returns the "L" variable

                Case "CHECKBOX"
                    If objControl.checked Then
                        vntValue = "YES"
                            'Returns the "L" variable
                    Else
                        vntValue = "NO"
                            'Returns the "L" variable
                    End If

                Case "RADIO"
                    '--NOTE: We are not using this control and instead using the RADIOBUTTON--
                    '--behavior control.
            End Select
                'UCase(objControl.type)

        Case "SELECT"
            'NOTE:This routine ASSUMES there is only one selected option in a list. Multiple
            'selections will not cause any problems - just the first selected option will be
            'returned. (tww 10/17/2000)
            If objControl.selectedIndex <> -1 Then
```
