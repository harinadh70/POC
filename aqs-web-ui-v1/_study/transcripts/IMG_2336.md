---
photo: IMG_2336.JPG
type: vscode-code
file: aqs-web-ui/src/features/auth/utils/middleware.ts
lines: 1-34
orientation: 180
confidence: high
notes: New tab opened "middleware.ts 5" (5 = problem count badge), next to "date.tsx 9+". Breadcrumb aqs-web-ui > src > features > auth > utils > middleware.ts. Explorer sidebar: middleware.ts (features/auth/utils, badge "5") highlighted/selected, siblings action.ts, loader.ts above it; another middleware.ts (features-level, no badge) visible just below the utils folder. Status bar: aqs-web-ui, hitanshu/experimental*, 30 errors / 0 warnings (up from 26), No Solution, Ln 1 Col 1, Tab Size 4, UTF-8, CRLF, TypeScript. Time overlay 4:46 PM 7/10/2026. Line 34 has a red "❌" emoji inline in the comment, mostly cut off by the status bar at the bottom edge but legible via zoom.
---
1: import { redirect } from 'react-router';
2: import { userContext } from '@/context';
3:
4: // utils
5: import { getItem } from '@utils/local-storage';
6:
7: // types
8: import type { MiddlewareFunction } from 'react-router';
9: import type { SessionInfo } from '@features/auth/services/auth';
10:
11: // ----------------------------------------
12:
13: // Client-side Authentication Middleware
14: export const authMiddleware: MiddlewareFunction = async ({ request, context }, next) => {
15:     // Check if user was already fetched and stored in context
16:     let userInfo = context.get(userContext);
17:
18:     if (!userInfo || !userInfo.userId) {
19:         // Try to get sessionInformation from localStorage using typed utility
20:         try {
21:             const sessionInfo = getItem<SessionInfo>('sessionInformation');
22:             if (sessionInfo && sessionInfo.userId) {
23:                 userInfo = sessionInfo;
24:                 // Store in context for further use
25:                 context.set(userContext, sessionInfo);
26:             }
27:         } catch (error) {
28:             console.error('Error getting sessionInformation from localStorage', error);
29:         }
30:     }
31:
32:     const url = new URL(request.url);
33:
34:     // ❌ if invalid userInfo kick out
