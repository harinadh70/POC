---
photo: IMG_4449.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (line numbers not shown in editor gutter)
orientation: 0
confidence: high
notes: |
  Same Visual Studio session as prior photos in this batch ("Main_ISLLSYS_20010101.vbs" active/gold,
  "Eebrowser.asp" inactive). Directly continues IMG_4448's Function Logout(): finishes the
  mdicChildWindows cleanup block, adds a further mdicNonPolicyChildWindows cleanup block, then calls
  LogoutGrandParent() (a THIRD, not-yet-fully-transcribed logout-family function referenced here),
  closes mobjParentWindow, sets mblnLogout = True, and ends Function Logout(). Immediately after, a new
  function begins: Function LogoutGrandParent() -- "This function logs out the user from AQS when
  Logout is clicked on a grand child page". This is the third page-tier-specific logout function seen so
  far in this file (LogoutMain for Home Page, Logout for Policy Windows page, LogoutGrandParent for a
  grand child page), reinforcing that Main_ISLLSYS_20010101.vbs is a shared top-level/frame-controller
  script implementing a whole family of page-tier logout handlers built on the same
  mdicExternalWindows/mdicChildWindows/mdicNonPolicyChildWindows window-tracking pattern. No line-number
  gutter enabled; indentation transcribed directly from a high-zoom crop of the photo and is
  high-confidence.
---
```vbs
                    Dim arrWindows, arrKeys, objWindow,strWindowName
                    arrWindows = mobjParentWindow.mdicChildWindows.Items
                    For intCount = 0 To mobjParentWindow.mdicChildWindows.Count - 1
                        Set objWindow = arrWindows(intCount)
                        If Not objWindow Is Nothing Then
                            If Not objWindow.close Then
                                objWindow.close
                            End If
                        End If
                    Next
                End If
                Call TrapBrowserError("LogoutChildWindow|2")
            End If
            If Not mobjParentWindow.mdicNonPolicyChildWindows Is Nothing Then
                If mobjParentWindow.mdicNonPolicyChildWindows.Count > 0 Then
                    arrWindows = mobjParentWindow.mdicNonPolicyChildWindows.Items
                    For intCount = 0 To mobjParentWindow.mdicNonPolicyChildWindows.Count - 1
                        Set objWindow = arrWindows(intCount)
                        If Not objWindow Is Nothing Then
                            If Not objWindow.close Then
                                objWindow.close
                            End If
                        End If
                    Next
                End If
                Call TrapBrowserError("LogoutChildWindow|3")
            End If
        End If
        LogoutGrandParent()
        mobjParentWindow.close
        Call TrapBrowserError("LogoutChildWindow|4")
    End If
    mblnLogout = True
    Call TrapBrowserError("LogoutChildWindow|5")
End Function
'--------------------------------------------------------------------------


Function LogoutGrandParent()
'****************************************************************************
'This function logs out the user from AQS when Logout is clicked on a grand child page
'****************************************************************************
    Dim arrWindows
    Dim objGrandParentWindow
```
