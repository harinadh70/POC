---
photo: IMG_4634.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible in editor; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4633, same tab "Main_ISLLSYS_20010101.vbs"
  (Eebrowser.asp inactive tab still present). Finishes the LTL-highlight
  If/Else block (mblnEnableLTLHighlight True/False), then a parallel
  "Get required highlighting setting, first time only" block setting
  mblnEnableRequiredHighlight via UserOption("reqhighlight","T",strUsrOpt).
  Then "--Condition the in-coming parameters--": strAttribute =
  UCase(strAttribute), strOrigValue = vntValue, followed by
  Select Case UCase(vntValue) with Case "T","Y" (boolean true conditions:
  vntValue=True, blnBooleanAttribute=True), Case "F","N" (boolean false
  conditions: vntValue=False, blnBooleanAttribute=True [note: verbatim as
  shown -- both T/Y and F/N branches set blnBooleanAttribute = True]), Case
  Else (blnBooleanAttribute = False), End Select. Then
  Call TrapBrowserError("EEBrowser|SetControlAttribute|1"). NOTE: no "End Sub"
  appears in this frame after the TrapBrowserError call, so SetControlAttribute
  is NOT yet finished here -- the actual VISIBLE/DISABLED attribute-application
  logic promised in the PURPOSE comment (IMG_4632) has not appeared yet;
  presumably continues in the next photo(s) of this batch, OR this
  TrapBrowserError call is a mid-routine checkpoint rather than the final one.
---
```vbs
        'Recheck for value (default needs to be disabled)
        If StrComp(strUsrOpt, "T", vbTextCompare) = 0 Then
            mblnEnableLTLHighlight = True
        Else
            mblnEnableLTLHighlight = False
        End If
    Else
        mblnEnableLTLHighlight = False
    End If
End If

'-- Get required highlighting setting, first time only
If IsEmpty(mblnEnableRequiredHighlight) Then
    If UserOption("reqhighlight", "T", strUsrOpt) Then
        'Recheck for value (default needs to be disabled)
        If StrComp(strUsrOpt, "T", vbTextCompare) = 0 Then
            mblnEnableRequiredHighlight = True
        Else
            mblnEnableRequiredHighlight = False
        End If
    Else
        mblnEnableRequiredHighlight = False
    End If
End If

'--Condition the in-coming parameters--
strAttribute = UCase(strAttribute)
strOrigValue = vntValue

Select Case UCase(vntValue)
    Case "T","Y"
        'All boolean true conditions
        vntValue = True
        blnBooleanAttribute = True
    Case "F","N"
        'All boolean false conditions
        vntValue = False
        blnBooleanAttribute = True
    Case Else
        blnBooleanAttribute = False
End Select

Call TrapBrowserError("EEBrowser|SetControlAttribute|1")
```
