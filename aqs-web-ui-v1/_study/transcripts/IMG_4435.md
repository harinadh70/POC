---
photo: IMG_4435.JPG
type: vbs-archive
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter shown)
orientation: 0
confidence: medium
notes: >
  Same Visual Studio session/tabs as IMG_4434 ("Main_ISLLSYS_20010101.vbs" pinned/inactive,
  "Eebrowser.asp" active). Scrolled down from IMG_4434 — visible region overlaps the tail
  of IMG_4434 (mlngTimeStartModalRequest, mlngTimeEndModalRequest, mlngTimeModalBOProcessing)
  then continues with new declarations. Still no line-number gutter visible.
  CORRECTION NOTE: in the raw photo, the right-hand comment column appears visually
  shifted up by one row relative to the left-hand Dim column (likely photo
  perspective/framing, not a real code structure change) — e.g. "Dim
  mlngTimeStartModalRequest" visually lines up with the comment "Processing time for the
  business object", which semantically belongs to mlngTimeBOProcessing instead. Verified
  against the unambiguous same-row pairing of these identical lines in IMG_4434 (where
  mlngTimeStartModalRequest clearly pairs with "Start time for MODAL call to a page") and
  reconstructed the correct Dim<->comment pairing below using semantic correspondence
  (comment text content) rather than raw pixel-row position. The literal "???" before
  "Return value from modal dialog window" is part of the source comment text itself, not
  a transcription illegibility marker. Two comment blocks explicitly name "EEBrowser.asp".
---
```vbscript
Dim mlngTimeStartModalRequest          ' Start time for MODAL call to a page (in milliSeconds)

Dim mlngTimeEndModalRequest            ' End time for MODAL call to a page - includes browser processing (in milliSeconds)

Dim mlngTimeModalBOProcessing          ' Processing time for the MODAL business object (in milliSeconds)

Dim mstrPolicyNumber                   ' For use on other pages to display the policy number

Dim mstrPrimaryInsured
Dim mstrTransactionType                ' For use on other pages to display the selected transaction type

Dim mstrReturnValue                    ' ???Return value from modal dialog window

Dim mblnDebug                          ' Show messages if true

Dim mvntDebugWindow                    ' Reference to the debug window.

Dim mblnTreeMessages                   ' Show messages related to tree changes

Dim mblnDeveloperMessages              ' Show messages related to page problems (html/xml)

Dim mblnNavigationMessages             ' Show the querystring prior to any action taken by the
                                        ' ExecuteAction sub. Option is given to cancel action.
                                        ' Use caution when cancelling a navigation from an enter/edit page.

Dim mblnDataChanged
'Application-wide boolean indicating that data on an Enter/Edit
'page has changed. This is managed by EEBrowser.asp.

Dim mblnRatingDataChanged
'Application-wide boolean indicating that data on an Enter/Edit
'page has changed that affects rating. This is managed by EEBrowser.asp.

Dim mstrExpiredNumber                  'Value that indicates history "POL|POL|(mstrExpiredNumber)|..."

Dim mobjParentWindow                   'Holds a reference to the window that opened this window.

Dim mdicChildWindows                   'Dictionary object, Holds references to the children windows opened by this window.
```
