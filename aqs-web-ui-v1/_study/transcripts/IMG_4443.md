---
photo: IMG_4443.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter numbers visible)
orientation: 0
confidence: high
notes: Visual Studio editor. Active tab "Eebrowser.asp"; tab "Main_ISLLSYS_20010101.vbs" still open. No line-number gutter visible. SIGNIFICANT - two TrapBrowserError calls explicitly reference "Main|window_onload|security" and "window_onload|menu1" as error-context labels, strongly corroborating that this window_onload logic is understood/labeled internally as part of "Main" and that a menu frame ("menu1") is loaded via ExecuteAction. Sections in this photo: closes "Configure the frames and borders"; "Specifiy the action" (sic, misspelled in source) - a Select Case CLng(mstrPolicyID) with only a Case -3 (mstrAction = "LIB") branch shown; "Load the security data island" - creates mxmlSecurity via MSXML2.DOMDocument.6.0, loads from document.all("xdiSecurity").XMLDocument.xml; "Set the src for the menu frame" - Call ExecuteAction("menu", "POL|POL|" & mstrExpiredNumber & "|", -1, ""). Trailing comment "After the menu page loads, it calls the LoadNextPage routine" begins next section, continues into next photo.
---
```vbs
            Else

                mblnSearchPage = True

                '--show dropdown only for utilites
                fraHorizontal.rows = "23,*"
                fraVertical.cols = "0,*"
                mstrTransactionType = ""

        End If
    '                                                       Configure the frames and borders
    '<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' >>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>>
    '                                                       Specifiy the action
        Select Case CLng(mstrPolicyID)

            Case -3
                mstrAction = "LIB"

        End Select
    '                                                       Specifiy the action
    '<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' >>>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>>
    '                                                       Load the security data island
    Set mxmlSecurity = CreateObject("MSXML2.DOMDocument.6.0")
    mxmlSecurity.async = False
    mxmlSecurity.preserveWhiteSpace = False
    mxmlSecurity.loadXML(document.all("xdiSecurity").XMLDocument.xml)
    Call TrapBrowserError("Main|window_onload|security")
    '                                                       Load the security data island
    '<<<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' >>>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>
    '                                                       Set the src for the menu frame
    Call ExecuteAction("menu", "POL|POL|" & mstrExpiredNumber & "|", -1, "")
    Call TrapBrowserError("window_onload|menu1")
    '                                                       Set the src for the menu frame
    '<<<<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' After the menu page loads, it calls the LoadNextPage routine
```
