---
photo: IMG_4556.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers shown; editor status bar reads "Ln: 2871 Ch: 26 MIXED")
orientation: 0
confidence: high
notes: >
  Same Visual Studio session (solution AQS.Advantage / AQS.Advantage.BatchEdit).
  The background tab name is now FULLY visible and confirmed as
  "Main_ISLLSYS_20010101.vbs" (previously truncated in IMG_4554/4555 to
  "...ain_ISLLSYS_20010101.vbs" / "...SYS_20010101.vbs"). This is a strong
  candidate for the top-level Main.vbs-style frame-controller script the task
  is hunting for -- it is not open/active in this photo (active tab remains
  "Eebrowser.asp"), so its contents are not yet transcribed; flagging for
  later photos in this batch in case that tab becomes active. Status bar
  cursor position still "Ln: 2871" (unchanged across IMG_4554-4556), so exact
  gutter line numbers for the visible code remain unknown/unconfirmed. Content
  continues the ExecuteBrowserCommands Select Case: finishes Case
  "CLICK_BUTTON", then a multi-value Case "UPDATE_TREENODETEXT",
  "UPDATE_TREENODEKEY", "REMOVE_TREENODE", "BUILD_TREE","UPDATE_TREENODEIMAGE"
  with a nested Select Case strVerb dispatching to methods on a "tree" frame
  document object (objDoc.UPDATE_TREENODETEXT, UPDATE_TREENODEKEY,
  REMOVE_TREENODE, BUILD_TREE, UPDATE_TREENODEIMAGE), then closes out the
  outer Select Case / If blocks with commented-out condition remnants
  ('UCase(strVerb), 'CStr(strResFil) = "" OR CStr(strResFil) = CStr(intResponseFilter),
  'mblnDoBrowserCommands) and begins a cleanup comment
  ("--Clean up the generic objects everytime through the loop--").
---

```vb
'==========================================

              Case "CLICK_BUTTON"
              '=====Simulate clicking an info button============
              'Get a handle to the object
              Call InfoButtonOnClick ( "dta" & strNoun , objCallingWindow )
                  Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|CLICK_BUTTON|1")
              '==========================================

              Case "UPDATE_TREENODETEXT", "UPDATE_TREENODEKEY", "REMOVE_TREENODE", "BUILD_TREE","UPDATE_TREENODEIMAGE"
              '=====Update the tree as required=====
              Set objDoc = document.frames("tree")

              Select Case strVerb
                  Case "UPDATE_TREENODETEXT"
                      Call objDoc.UPDATE_TREENODETEXT ( Escape(strNoun) , Escape(strAddInf) )
                      '==========================================
                  Case "UPDATE_TREENODEKEY"
                      Call objDoc.UPDATE_TREENODEKEY ( Escape(strNoun) , Escape(strAddInf) )
                      '==========================================
                  Case "REMOVE_TREENODE"
                      Call objDoc.REMOVE_TREENODE ( Escape(strNoun) , Escape(strAddInf) )
                      '==========================================
                  Case "BUILD_TREE"
                      Call objDoc.BUILD_TREE ( Escape(strNoun) , Escape(strAddInf) )
                      '==========================================
                  Case "UPDATE_TREENODEIMAGE"
                      Call objDoc.UPDATE_TREENODEIMAGE ( Escape(strNoun) , Escape(strAddInf) )
                      '==========================================
              End Select
              Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|TREENODECOMMANDS|1|" & strVerb)
              '==========================================

        End Select
              'UCase(strVerb)
    End If
              'CStr(strResFil) = "" OR CStr(strResFil) = CStr(intResponseFilter)
End If
    'mblnDoBrowserCommands

Call TrapBrowserError("EEBrowser|ExecuteBrowserCommands|2")

'--Clean up the generic objects everytime through the loop--
```
