---
photo: IMG_4502.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; status bar cut off at bottom edge of photo)
orientation: 0
confidence: high
notes: >
  Active tab is "Main_ISLLSYS_20010101.vbs" (tan/pinned, bold black text); "Eebrowser.asp" inactive
  (dark navy) -- consistent with IMG_4500/IMG_4501. Content: tail end of Function Tree_SelectNode
  (delegates to document.frames("tree").Tree_SelectNode), End Function, then a NEW function
  "SetExpiredNumber(ByVal pstrNodeKey)" which parses a pipe-delimited node-key string of form
  "(lob)|(second value)|(expired number)|(additional values...)" e.g. "POL|POLLVL|0|" -- introduces
  local vars intCount, strTempString, intStart, intEnd and uses InStr to locate the 1st/2nd/3rd "|"
  delimiters. Function body is cut off mid-comment at bottom of frame ("'If the second "|" is past
  where the first "|" should be and 'the third "|" is past where the second "|" is...").
---
```vbs
    'Returns true if the node is found.
'*****************************************************************************
    Dim objDoc
    On Error Resume Next
    'Default
    Tree_SelectNode = False

    Set objDoc = document.frames("tree")

        If Not objDoc Is Nothing Then
            Tree_SelectNode = objDoc.Tree_SelectNode(CStr(pstrNodeKey))
            Set objDoc = Nothing
        End If
    Call TrapBrowserError("Tree_SelectNode|1")

End Function
'------------------------------------------------------------------------------------------

Function SetExpiredNumber(ByVal pstrNodeKey)
'*****************************************************************************
    'This function replaces the third item in the node key with the current
    'value of mstrExpiredNumber.  The node key is assumed to be in the form:
    '  (lob)|(second value)|(expired number)|(additional values...)
    ' The first field should always be 3 characters.
    ' There should always by at least three "|" characters. ie: POL|POLLVL|0|
'*****************************************************************************
    Dim intCount
    Dim strTempString
    Dim intStart
    Dim intEnd

    On Error Resume Next

    strTempString = pstrNodeKey
    intStart = InStr(1,strTempString,"|")
        'position of first  "|"
    intStart = InStr(intStart + 1,strTempString,"|")
        'position of second "|"
    intEnd = InStr(intStart + 1,strTempString,"|")
        'position of third  "|"

    'If the second "|" is past where the first "|" should be and
    'the third "|" is past where the second "|" is...
```
