---
photo: IMG_4530.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter shown; editor status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  IMPORTANT: This is NOT the StarTeam/DevOps archive viewer used in prior photos -- this is
  Microsoft Visual Studio (menu bar File/Edit/View/Git/Project/Build/Debug/Team/Test/Analyze/Tools/
  Extensions/Window/Help; toolbar shows Debug/Any CPU, project "AQS.Advantage.BatchEdit", solution
  "AQS.Advantage"; bottom-right shows git branch "develop" and repo "aqs"). The ACTIVE/focused tab is
  "Main_ISLLSYS_20010101.vbs" (gold highlighted tab) -- this is a candidate for the "Main.vbs"-style
  top-level file the task asked to watch for, though its exact name is "Main_ISLLSYS_20010101.vbs"
  (looks like a dated/site-specific filename, possibly ISLLSYS = a client/site code, "20010101" a
  version/date stamp). A second, INACTIVE background tab reads "Eebrowser.asp" (note .asp extension
  here, not .vbs as used for the file transcribed in the prior batch -- may be the same logical file
  under a different extension, or a distinct file; not confirmed since it is not the active tab).
  The code-outline dropdown (top right of editor) shows this cursor position is inside
  "Sub window_onbeforeunload". The visible code is variable declarations (Dim statements) with
  descriptive comments laid out in what appears to be a two-column visual arrangement (comment text
  offset well to the right of the Dim statement it may describe); exact logical pairing between each
  comment and its Dim is not fully certain from the photo, so lines are transcribed in on-screen
  top-to-bottom reading order without asserting which comment documents which variable. The comment
  after "Dim intCount" begins "'in mxmlBrowserCtl() from the server." (lowercase "in", i.e. this is
  the tail of a sentence whose first line is scrolled off above the top of the photo). This same
  window_onbeforeunload code (mxmlBrowserCtl, objCallingWindow, DISPLAY_QUESTION, functional security
  comments) strongly resembles the style/content seen in Eebrowser.vbs's window_onload region from the
  prior batch -- worth comparing once both are fully transcribed to see if Main_ISLLSYS_20010101.vbs
  and Eebrowser share a common window-event-handling pattern or codebase lineage.
---
```vbs
    Dim intCount
                                                    'in mxmlBrowserCtl() from the server.

    Dim intIndex
                                                    'Generic integer counter.

    Dim strQS
                                                    'Integer used to vary the starting index when loading a
                                                    'combo box with items.

    Dim intResponseFilter
                                                    'The querystring portion of the URL.

    Dim objMessageBox
                                                    'The button pressed in response to a DISPLAY_QUESTION
                                                    'browser command.

    Dim strProgramType,strPageMatchCode

    Dim blnSecDisabled, blnSecVisible
                                                    ' used for functional security

                                                    ' used for functional security

    Dim objListXML
    Dim objItems
    Dim strShowZeroText

    Dim objCalls
    ' Local variables to hold values for each command as it executes
    Dim strVerb
    Dim strNoun
    Dim strAddInf
    Dim strResFil

    With objCallingWindow
        '--Spin thru the returned Browser Control xml and respond to ALL commands--
        '--from the server
        '-- check for browser commands xml

        If Not .mxmlBrowserCtl Is Nothing And Not IsEmpty(.mxmlBrowserCtl) Then

            ' Get all nodes named <call>
            Set objCalls = .mxmlBrowserCtl.selectNodes("//call")

            For intRtnCount = 0 To objCalls.length - 1

                strVerb =              UnEscapeTextValueForXML(objCalls(intRtnCount).attributes.GetNamedItem("verb").nodeValue)
                strNoun =              UnEscapeTextValueForXML(objCalls(intRtnCount).attributes.GetNamedItem("noun").nodeValue)
```
