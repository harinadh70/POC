---
photo: IMG_4527.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter line numbers shown; status bar reads Ln 2871, Ch 26 -- unchanged)
orientation: 0
confidence: medium
notes: >
  Directly continues IMG_4526's Case "OBJECT" block. Finishes the Sheridan Tree Control ElseIf,
  then a third branch "'--IE8 Tree Control--" ElseIf checking classid against
  "../../SYSTEM/WINCONTROLS/AQSCONTROLLIBRARY1.DLL#AQSCONTROLLIBRARY.TREEVIEW" -- this line's
  trailing "= 0 Then" is NOT visible in the photo (line appears truncated at the right edge of
  the photographed frame, confirmed by close crop; by parallelism with the two ElseIf branches
  immediately above it almost certainly continues ", vbTextCompare) = 0 Then" off-frame, but this
  is not directly legible so marked with «?»). Closes Case "OBJECT" (End If / End If), then new
  Case "INPUT","RADIOBUTTON","TEXTAREA","COMBO","KPCOMBO","DIV" (calendar-attribute check via
  objCallingWindow.CheckDate/FormatDate, else required-value-not-empty check), Case "XMLLIST",
  "IMDBXMLLIST" (RowCount < 1 check), Case "IBUTTON" (ClickedOnce check), End Select. Same
  Visual Studio session; navigator still class member dropdown context from IMG_4526 (not
  re-photographed this frame). Confirms this is the body of Function CheckSpecifiedIndicator
  (see IMG_4528 for "CheckSpecifiedIndicator = blnSuccess" / End Function).
---
```vbs
                                            blnUpdateList = True
                                        End If
                                    '--IE8 Tree Control--
                                    ElseIf StrComp(objControls(intIndex).classid, "../../SYSTEM/WINCONTROLS/AQSCONTROLLIBRARY1.DLL#AQSCONTROLLIBRARY.TREEVIEW", vbTextCompare)«?»
                                        ' calling page must contain the function mblnTreeItemSelected()
                                        If Not .mblnTreeItemSelected Then
                                            blnSuccess = False
                                                'Mark our flag as failed
                                            blnUpdateList = True
                                        End If
                                    End If

                        Case "INPUT","RADIOBUTTON","TEXTAREA","COMBO","KPCOMBO","DIV"
                                            If Not IsNull(objControls(intIndex).getAttribute("iscalendar")) Then
                                                If Not objCallingWindow.CheckDate(objCallingWindow.FormatDate(GetControlValue(objControls(intIndex)))) Then
                                                    blnSuccess = False
                                                    blnUpdateList = True
                                                End If
                                            Else
                                                If Len(Trim(GetControlValue(objControls(intIndex)))) = 0 Then
                                                    '--The value is empty, so add this to the return list--
                                                    '--and indicate failure.
                                                    blnSuccess = False
                                                        'Mark our flag as failed
                                                    blnUpdateList = True
                                                End If
                                            End If
                                            'IfGetControlValue(objControls(intIndex)) = ""

                        Case  "XMLLIST", "IMDBXMLLIST"
                                            If objControls(intIndex).RowCount < 1 Then
                                                blnSuccess = False
                                                    'Mark our flag as failed
                                                blnUpdateList = True
                                            End If
                                            'GetControlValue(colXMLLists(intIndex)) = ""
                        Case "IBUTTON"
                                            If Not objControls(intIndex).ClickedOnce Then
                                                blnSuccess = False
                                                blnUpdateList = True
                                            End If
                    End Select
```
