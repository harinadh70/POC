---
photo: IMG_4641.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; status bar cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4638-4640 (same editor state). Finishes Case "INPUT","SELECT","TEXTAREA"
  DISABLED handling (pale-yellow vs default backgroundcolor logic keyed on UCase(.type)<>
  "BUTTON" And <>"CHECKBOX", tag |15), adds Case "READONLY" (tag |16), then "End Select /
  'UCase(.tagname)" closes the big tag-name Select Case block that started before IMG_4638.
  Immediately after, an "Else / 'Not a Boolean Attribute" branch begins a NEW Select Case
  strAttribute block (this is the non-boolean-attribute counterpart of the boolean-attribute
  block transcribed in IMG_4638-4641) with Case "REQUIRED" and Case "MAXLENGTH", both generic
  handlers using .SetAttribute "required"/"maxlength", vntValue, 0 with near-identical comment
  text for every control type. This confirms the overall routine is a generic
  SetControlAttribute-style dispatcher: outer If/Else splits boolean vs non-boolean attributes,
  inner Select Case .tagname / Select Case strAttribute drive the boolean branch already seen.
---
```vbnet
        Case "DISABLED"
            .disabled = vntValue

            If UCase(.type) <> "BUTTON" And UCase(.type) <> "CHECKBOX" Then
                If vntValue Then
                    'Set backcolor to pale yellow
                    .style.backgroundcolor = mhexDisabledColor
                Else
                    'Set backcolor to default (usually white)
                    .style.backgroundcolor = ""
                End If
            End If
                'UCase(.type) <> "BUTTON"
            Call TrapBrowserError("EEBrowser|SetControlAttribute|15")
        Case "READONLY"
            .disabled = vntValue
            If vntValue Then
                'Set backcolor to default (usually white)
                .style.backgroundcolor = ""
            End If
            Call TrapBrowserError("EEBrowser|SetControlAttribute|16")
        End Select
            'strAttribute

    End Select
        'UCase(.tagname)

Else
    'Not a Boolean Attribute
    Select Case strAttribute
        Case "REQUIRED"
            'This is the Required attribute that should be on EVERY control.  So, we
            'don't need to do anything different for any type of control - just change
            'the HTML attribute value.
            .SetAttribute "required", vntValue, 0

        Case "MAXLENGTH"
            'This is the Maxlength attribute that should be on EVERY control.  So, we
            'don't need to do anything different for any type of control - just change
            'the HTML attribute value.
            .SetAttribute "maxlength", vntValue, 0

    End Select
```
