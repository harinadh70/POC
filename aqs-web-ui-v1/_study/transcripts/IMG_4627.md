---
photo: IMG_4627.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible in editor; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4626 -- same Visual Studio window, same active tab
  "Main_ISLLSYS_20010101.vbs" (inactive tab "Eebrowser.asp" still open in
  background). This picks up right after the COMBO/KPCOMBO case's intOffset
  logic, finishes that Case block, then begins "Case "INPUT"" with a nested
  Select Case UCase(objControl.type) / Case "TEXT" handling a calendar/date
  input control (iscalendar attribute), splitting a date string on "/" into
  marrEEData(2)/(3)/(4)/(6). Confirms this large file (Ln 2871 shown in status
  bar) continues the same marrEEData-population pattern seen in Eebrowser.vbs's
  window_onload, but under the distinct filename Main_ISLLSYS_20010101.vbs.
---
```vbs
' that is not limitToList, so the .listindex in this first condition will always be -1
If Len(objControl.value) = 0 Then
        .marrEEData(3) = objControl.listindex + intOffset
Else
        .marrEEData(3) = objControl.value
End If

.marrEEData(7) = objControl.listindex + intOffset
.marrEEData(4) = objControl.value

Case "INPUT"
    Select Case UCase(objControl.type)
        Case "TEXT"
            If Not IsNull(objControl.getAttribute("iscalendar")) Then
                If UCase(objControl.iscalendar) = "T" Then
                    .marrEEData(2) = GetControlValue(objControl)
                    If Len(.marrEEData(2)) > 0 Then
                        arrDate = Split(.marrEEData(2), "/")
                        If UBOUND(arrDate) < 2 then
                            objControl.value = .FormatDate(objControl.value)
                            .marrEEData(2) = GetControlValue(objControl)
                            arrDate = Split(.marrEEData(2), "/")
                        End If
                    .marrEEData(6) = .marrEEData(2)
                    .marrEEData(2) = arrDate(0)
                    .marrEEData(3) = arrDate(1)
                    .marrEEData(4) = arrDate(2)
                Else
                    .marrEEData(2) = ""
                    .marrEEData(3) = ""
                    .marrEEData(4) = ""
                    .marrEEData(6) = ""
                End If
            else
                .marrEEData(2) = GetControlValue(objControl)
                .marrEEData(3) = ""
                .marrEEData(4) = ""
            end if
        else
            .marrEEData(2) = GetControlValue(objControl)
            .marrEEData(3) = ""
            .marrEEData(4) = ""
        end if
```
