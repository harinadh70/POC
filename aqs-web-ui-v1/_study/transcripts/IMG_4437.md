---
photo: IMG_4437.JPG
type: vbs-archive
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter shown)
orientation: 0
confidence: high
notes: >
  Same Visual Studio session/tabs as IMG_4434-4436 ("Main_ISLLSYS_20010101.vbs"
  pinned/inactive, "Eebrowser.asp" active). SIGNIFICANT: two comments in this photo
  explicitly name "Main_ISLLSYS_20010101.asp" as a real, separate file that sends values
  to this one ("Initialize Insured Account Number if Main_ISLLSYS_20010101.asp does not
  send a value" / same for Policy Number). This corroborates that Main_ISLLSYS_20010101 is
  a top-level caller/frame-controller script — the open-but-inactive editor tab is titled
  "Main_ISLLSYS_20010101.vbs" while the in-code comment references it with a ".asp"
  extension; both names likely refer to the same logical script (tab title may be showing
  a renamed/converted copy). This is the clearest lead so far for the "Main.vbs /
  AQSMain.vbs-like top-level script" being sought. Top of visible region repeats the tail
  of IMG_4436 (mblnEnableRequiredHighlight / mblnNoAssiDoc comments) — omitted here to
  avoid duplication; this repeat also confirms the semantic (not same-row) Dim<->comment
  pairing method used for IMG_4436 was correct. "SetContorlAttribute" is a verbatim typo
  in the source comment (not a transcription error). Content moves from simple Dim/comment
  pairs into a block of color-related Dims with short inline sub-comments, then a long
  multi-line comment block introducing "mstrNextAction_*" variables used to hold browser
  command navigation info for ExecuteAction, cut off at the bottom mid-declaration
  (continues into next photo). Still no line-number gutter visible.
---
```vbscript
Dim mIANumber                          'Initialize Insured Account Number if Main_ISLLSYS_20010101.asp does not send a value

Dim mPNumber                           'Initialize Policy Number if Main_ISLLSYS_20010101.asp does not send a value

Dim mblnDoNotSetFocusToParent
mblnDoNotSetFocusToParent = false

Dim mstrBatchEditWindowDisplayed
mstrBatchEditWindowDisplayed = ""

    ' Colors used for setting disabled control color in SetContorlAttribute
    ' ( from objMenu.mColorCtlDisabledDec )
Dim mintDisabledRed
Dim mintDisabledGreen
Dim mintDisabledBlue
    ' ( from objMenu.mColorCtlDisabledHex )
Dim mhexDisabledColor
Dim mhexDisabledLabelColor
Dim mhexRequiredLabelColor
    ' ( from objMenu.mColorCtlComboLTLFalseHex... )
Dim mhexComboLTLFalseColor
Dim mhexComboLTLTrueColor
    ' ( from objMenu.mColorRequiredBackgroundHex... )
Dim mhexRequiredBackgroundColor
    ' ( from objMenu.mColorRequiredBackgroundDec... )
Dim mintRequiredBackgroundRed
Dim mintRequiredBackgroundGreen
Dim mintRequiredBackgroundBlue

' Variables used to hold browser command navigation info for ExecuteAction call
' to be done after modal closes (launched by ExecuteAction)
' This allows the modal to close before launching the next page.
' (which may be a modal!!)
' These variables are first set to "" in ExecuteAction and then set in
' ExecuteBrowserCommands (NAVIGATE_CYCLING case)
' by calling SetNextAction if the calling window is a modal and was launched
' by ExecuteAction (frame = modal)
Dim mstrNextAction_Action                                      ' = pstrAction
```
