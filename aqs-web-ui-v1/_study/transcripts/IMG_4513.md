---
photo: IMG_4513.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4512, same VS session. Shows "End Function" (closing
  ShowUserMessage from prior photos), then a full "Sub SetNextAction(ByVal
  pstrAction, ByVal pstrNodeKey, ByVal pintTab, ByVal pstrXMLDetail)" with a
  detailed banner comment explaining it stores values from browser command
  NAVIGATE_CYCLING as module-level variables (mstrNextAction_Action,
  mstrNextAction_NodeKey, mstrNextAction_Tab, mstrNextAction_XMLDetail) for
  later use by "ExecuteAction" -- confirms an ExecuteAction routine exists
  elsewhere in this file (not yet photographed). The banner comment also shows
  example caller values referencing ".marrSessionInformation(4)",
  "strMatchCode", "strNodeKey", "intTab", "Escape(.marrSessionInformation(6))"
  -- i.e. this Sub is called from within a "With ..." block elsewhere using
  session-information array elements. Then begins "Sub GetCyclingValues (ByVal
  pstrAction, ByVal pstrNodeKey, ByVal pintTab, ByVal pstrXMLDetail, ByRef
  pstrFrame, ByRef pstrURL, ByRef pstrModalSize)" with banner header "PURPOSE:"
  (comment cut off / continues past visible page) and declares objResponse,
  objNode ('XML document to hold the xml of the response... node within
  ("/navigate") from response), strURL, strQueryString ('New url as specified
  by the cycling component), followed by a multi-line comment explaining
  UnEscape/Escape handling for querystring values (comment text continues
  past bottom edge of photo, not fully captured).
---
```vb
End Function
'--------------------------------------------------------------------------------------

Sub SetNextAction(ByVal pstrAction, ByVal pstrNodeKey, ByVal pintTab, ByVal pstrXMLDetail)
'****************************************************************************
' Takes the values from the browser command NAVIGATE_CYCLING and
' stores them as module level variable for use by ExecuteAction to call
' ExecuteAction ( yes ExecuteAction) to perform the next navigation to
' allow the modal to close before launching the next page/modal.

' pstrAction = .marrSessionInformation(4) & "|" & strMatchCode,
'   pstrNodeKey = strNodeKey,
'   pintTab = intTab,
'   pstrXMLDetail = Escape(.marrSessionInformation(6))
'****************************************************************************

    mstrNextAction_Action                  = pstrAction
    mstrNextAction_NodeKey                 = pstrNodeKey
    mstrNextAction_Tab                     = pintTab
    mstrNextAction_XMLDetail               = pstrXMLDetail

End Sub
'--------------------------------------------------------------------------------------

Sub GetCyclingValues (ByVal pstrAction, ByVal pstrNodeKey, ByVal pintTab, ByVal pstrXMLDetail, ByRef pstrFrame, ByRef pstrURL, ByRef pstrModalSize)
'****************************************************************************
'PURPOSE:
'‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
'****************************************************************************
On Error Resume Next

    Dim objResponse

    Dim objNode                                    ' XML document to hold the xml of the response

    Dim strURL                                     ' node within ("/navigate") from response

    Dim strQueryString                             ' New url as specified by the cycling component

    'The reason for UnEscaping the passed parameter is to make sure when it is
    'escaped later in a querystring value, that it doesn't become escaped twice.
    'Note that this is effectively equivalent to what happens when a querystring value
    'that had been escaped is read from the request.querystring object - it is
```
