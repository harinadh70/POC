---
photo: IMG_4515.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter visible)
orientation: 0
confidence: high
notes: >
  MAJOR FINDING. Continuation of IMG_4514, finishes Sub GetCyclingValues
  (reads pstrFrame, pstrURL, pstrModalSize from the XmlCycling.aspx response,
  nulls objNode/objResponse, End Sub). Immediately after, a prominent banner
  comment block reads: "'Functions moved from EEBrowser.asp" (note
  capitalization "EEBrowser.asp" here vs "Eebrowser.asp"/"Eebrowser.vbs"
  elsewhere -- Windows filesystems are case-insensitive so this is likely the
  same file, just typed inconsistently over time). This is DIRECT EVIDENCE
  that Main_ISLLSYS_20010101.vbs was assembled in part by moving functions out
  of EEBrowser.asp and into this frame-controller file -- i.e. the two files
  are historically/structurally linked, with logic migrating from the
  page-level Eebrowser script into the top-level Main-style controller. Right
  after the banner, "Sub ApplySecurityPolicy(strFieldMatchCode,
  objCallingWindow)" begins -- this is presumably one of the relocated
  functions -- declaring strProgramType, strPageMatchCode, nodPageMatchCode,
  nodSecurity, strVisible, strDisabled, strSecurityPath, objControl,
  colControls, objTempControl (declaration list continues past bottom of
  photo).
---
```vb
    strURL = URLDecodeJS(GetNodeText(objNode, "url",""))

    pstrFrame = Ucase(GetNodeText(objNode, "frame",""))

    pstrURL = strURL & strQueryString

    pstrModalSize = GetNodeText(objNode, "height", "") & "," & GetNodeText(objNode, "width", "")
    If StrComp(pstrModalSize, ",", vbTextCompare) = 0 Then
        pstrModalSize = ""
    End If

    End If

    Call TrapBrowserError("GetCyclingValues|6")

    Set objNode = Nothing
    Set objResponse = Nothing

End Sub
'--------------------------------------------------------------------------------------

'****************************************************************************
'****************************************************************************
'Functions moved from EEBrowser.asp
'****************************************************************************
'****************************************************************************
'--------------------------------------------------------------------------------------

Sub ApplySecurityPolicy(strFieldMatchCode, objCallingWindow)
'****************************************************************************
'****************************************************************************
    Dim strProgramType
    Dim strPageMatchCode
    Dim nodPageMatchCode
    Dim nodSecurity
    Dim strVisible
    Dim strDisabled
    Dim strSecurityPath
    Dim objControl
    Dim colControls
    Dim objTempControl
```
