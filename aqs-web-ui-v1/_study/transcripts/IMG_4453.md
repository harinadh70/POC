---
photo: IMG_4453.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (line numbers not shown in editor gutter)
orientation: 0
confidence: high
notes: |
  Same Visual Studio session as prior photos ("Main_ISLLSYS_20010101.vbs" active/gold, pinned; "Eebrowser.asp"
  inactive). Continues directly from IMG_4452: finishes Function ShowConversionReport (window.showModelessDialog
  call, resets mlngConversionReportID = 0, Call TrapBrowserError("ShowConversionReport|1"), End Function), then
  two NEW subs begin: Sub LoadWindowReferences (ByVal pstrKey, ByRef pobjWindow) -- "This sub is used to pass the
  Dictionary object between the parent and child windows. This is done to preserve the references to open policy
  windows." -- and Sub LoadNonPolicyWindowReferences (ByVal pstrKey, ByRef pobjWindow) -- same pattern but for
  "non policy child windows" and mdicNonPolicyChildWindows. Both follow an identical pattern: lazily
  CreateObject("Scripting.Dictionary") into the relevant mdic* global if Nothing, remove any existing item for
  pstrKey ("it may be an old copy"), then Add the new pobjWindow reference keyed by pstrKey. Sidebar shows a
  "Data Sources" vertical tab now visible on the far left edge (as in IMG_4446). Reinforces that
  Main_ISLLSYS_20010101.vbs centrally owns three parallel window-tracking dictionaries (mdicChildWindows,
  mdicNonPolicyChildWindows, and mdicExternalWindows seen earlier) plus matching Load*WindowReferences setter
  subs. No line-number gutter enabled. Last visible line (If mdicNonPolicyChildWindows.Exists (CStr(pstrKey))
  Then) is cut off at the bottom of frame, continuing onto the next photo.
---
```vbs
        window.showModelessDialog (strURL), window, (strWindowOptions)

        mlngConversionReportID = 0
    End If
    Call TrapBrowserError("ShowConversionReport|1")
End Function
'--------------------------------------------------------------------------------------------------------------------------

Sub LoadWindowReferences (ByVal pstrKey, ByRef pobjWindow)
'****************************************************************************
'This sub is used to pass the Dictionary object between the parent and child
'windows.  This is done to preserve the references to open policy windows.
'****************************************************************************
On Error Resume Next

    If mdicChildWindows Is Nothing Then
        Set mdicChildWindows = CreateObject("Scripting.Dictionary")
    End If

    'Remove the item if it exists, it may be an old copy.
    If mdicChildWindows.Exists (CStr(pstrKey)) Then
        mdicChildWindows.Remove (CStr(pstrKey))
    End If

    mdicChildWindows.Add CStr(pstrKey), pobjWindow
    Call TrapBrowserError("LoadWindowReferences|1")
End Sub
'--------------------------------------------------------------------------------------------------------------------------

Sub LoadNonPolicyWindowReferences (ByVal pstrKey, ByRef pobjWindow)
'****************************************************************************
'This sub is used to pass the Dictionary object between the parent and non
'policy child windows.  This is done to preserve the references to open non
'policy windows.
'****************************************************************************
On Error Resume Next
    If mdicNonPolicyChildWindows Is Nothing Then
        Set mdicNonPolicyChildWindows = CreateObject("Scripting.Dictionary")
    End If

    'Remove the item if it exists, it may be an old copy.
    If mdicNonPolicyChildWindows.Exists (CStr(pstrKey)) Then
```
