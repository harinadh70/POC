---
photo: IMG_4474.JPG
type: vbs-archive
file: Main_ISLLSYS_20010101.vbs (active tab; Eebrowser.asp also open as inactive background tab)
lines: unknown (no line-number gutter shown in this editor view; status bar shows cursor at Ln 1405, Ch 4, Col 13, MIXED)
orientation: 0
confidence: high
notes: >
  Direct continuation of IMG_4473 -- first two lines ("strURLNoQueryString = strURL" /
  "strURL = strURL & strQueryString") exactly match the last two lines transcribed there. Same
  Main_ISLLSYS_20010101.vbs active tab. Content: clears document.all("MAIN").src to loading.htm unless the
  target frame is MODAL/NEWWINDOW (with an explanatory banner comment about avoiding flash of old content but
  not clobbering an open modal). Initializes mstrNextAction_Action/_NodeKey/_Tab/_XMLDetail and
  mblnNextAction_DeferNavigation to prevent a navigation loop, then begins
  "Select Case UCase(strFrame)" with cases "NOTCONVERTED" (shows a "usermessage_v2conversion" error via
  ShowUserMessage/UserValue) and "MODAL","MODELESS" (opens a modal dialog window using a URL passed via a
  "ModalURL" querystring value; comment flags "**CODE REQUIRED**"; reads intHeight/intWidth from
  objNode "height"/"width" with defaults 500/600). This Select Case strongly reinforces that
  Main_ISLLSYS_20010101.vbs is the frame/window controller that decides whether the next page renders in a
  frame, a modal dialog, or a new window -- exactly the kind of top-level "Main.vbs" dispatch logic flagged
  as a target for this batch.
---

```vbs
strURLNoQueryString = strURL
strURL = strURL & strQueryString

'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
'NOTE: The main frame needs to be cleared.
'           This eliminates flashing of old content when another page
'           is called for the main frame.  The main frame should not be cleared
'           if the page being called is a modal since cancelling the modal
'           then leaves the loading.htm in the main frame
'~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

If UCase(strFrame) <> "MODAL" And UCase(strFrame) <> "NEWWINDOW" Then
    document.all("MAIN").src = "../../system/htm/loading.htm"
End If

Call TrapBrowserError("ExecuteAction|6")

' Initialize these to prevent loop
mstrNextAction_Action              = ""
mstrNextAction_NodeKey             = ""
mstrNextAction_Tab                 = ""
mstrNextAction_XMLDetail           = ""
mblnNextAction_DeferNavigation = False
' Determine if the new page should be shown as modal, in a frame or new window.
Select Case UCase(strFrame)
    Case "NOTCONVERTED"
        Call ShowUserMessage ( UserValue("usermessage_v2conversion","message"), "ERROR", "1" )

    Case "MODAL", "MODELESS"

        ' Open a modal dialog window

        ' Open the URL passed to the browser via the querystring
        ' "ModalURL" in a modal dialog window
        ' **CODE REQUIRED**
        Dim intHeight
        Dim intWidth

        ' Look for the size of the modal dialog
        ' Set the sizes
        intHeight = GetNodeText(objNode, "height", "500")
        intWidth = GetNodeText(objNode, "width", "600")
```
