---
photo: IMG_4571.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4570, same VS window/tabs (Main_ISLLSYS_20010101.vbs inactive, Eebrowser.asp active). Closes out
  GetControlAttribute (End Select/End With/TrapBrowserError("EEBrowser|GetControlAttribute|2")/End Function), then a
  dashed divider, then begins Function GetControlValue(ByRef objControl) with its PURPOSE/PARAMETERS header comment.
  Body starts: Dim vntValue, If Not objControl Is Nothing Then / Select Case UCase(objControl.tagname) / Case "OBJECT" /
  Select Case UCase(objControl.classid) / Case "CLSID:CC696B63-4159-11D0-BDCB-0020A90B183A" (Protoview Date/Time Control)
  -- vntValue = objControl.datestring, with a commented-out alternative `'vntValue = objControl.value 'Returns a date`.
  No AQSMain/Main.vbs content visible. No line-number gutter.
---
```vbscript
                    End If
                        '.disabled
                End Select
                    'strAttribute
        End Select
            'UCase(.tagname)
    End With
        'objControl
    Call TrapBrowserError("EEBrowser|GetControlAttribute|2")
End Function
'------------------------------------------------------------------------------

Function GetControlValue(ByRef objControl)
'******************************************************************************
'PURPOSE:
'The purpose of this function is to return the current "L" or "Text" value
'of the control referenced in the parameter. This should be the ONLY way to
'reference the current "Text" value of a control, so as to keep the code as
'generic as possible.
'
'Since many HTML and ActiveX controls have a different "value property",
'it is best to specifically specify each control type and it's appropriate
'property.
'
'PARAMETERS:
'objControl: An object reference to a control
'
'******************************************************************************
On Error Resume Next

Dim vntValue                           'The current value of the desired control.

If Not objControl Is Nothing Then
    '--Get the appropriate value for the type of control--
    Select Case UCase(objControl.tagname)
        Case "OBJECT"
            Select Case UCase(objControl.classid)
                Case "CLSID:CC696B63-4159-11D0-BDCB-0020A90B183A"
                    '--Protoview Date/Time Control--
                    vntValue = objControl.datestring
                        'Returns the date as string w/no time
                    'vntValue = objControl.value    'Returns a date
```
