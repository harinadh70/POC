---
photo: IMG_4660.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26 -- unchanged from prior photos in this run)
orientation: 0
confidence: high
notes: >
  Same VS session/tabs as IMG_4650-4659. Completes Function ClearXMLDetail: after the
  removeChild loop and resetting mstrXMLDetail, does the cleanup promised by the prior
  comment -- Replace(mstrXMLDetail, Chr(34), "'") (double->single quote),
  Replace(...,Chr(10),"") and Replace(...,Chr(13),"") (strip LF/CR) -- sets
  ClearXMLDetail = True (success), then NOTABLE: calls
  Call TrapASPError("ClearXMLDetail|1") -- this is "TrapASPError", NOT
  "TrapBrowserError" used by every other routine transcribed in this batch
  (SetControlValue, BuildTab, ShowZeroText, etc.). This suggests ClearXMLDetail is
  reachable/relevant in a server-side ASP execution context (or this file is a shared
  .vbs included both client-side in the browser and server-side in classic ASP),
  distinct from the purely client-side (window/document-driven) routines seen earlier
  in this batch. Sets xmlSessionXML = Nothing and ends with End Function. This is the
  last visible content in the current scroll position for this batch of photos.
---

```vbs
        If xmlSessionXML.loadXML(mstrXMLDetail) = False Then
          Exit Function
        End If

    For x = 0 To Ubound(arrXMLDetail)
        strXSL = "//item[@name='" & arrXMLDetail(x) & "']"
        Set nodItem = xmlSessionXML.selectSingleNode(strXSL)
        If nodItem Is Nothing Then
          'Do Nothing
        Else
          xmlSessionXML.documentElement.removeChild nodItem
          Set nodItem = Nothing
        End If
    Next

    ' Reset the xml string.
    mstrXMLDetail = xmlSessionXML.xml

    ' Clean up mstrXMLDetail:
    ' Replace double quotes with single quotes.
    ' Remove carriage returns and line feeds.

    mstrXMLDetail = Replace(mstrXMLDetail, Chr(34), "'")
    mstrXMLDetail = Replace(mstrXMLDetail, Chr(10), "")
    mstrXMLDetail = Replace(mstrXMLDetail, Chr(13), "")

    'Success
    ClearXMLDetail = True

    Call TrapASPError("ClearXMLDetail|1")
    Set xmlSessionXML = Nothing

End Function
```
