---
photo: IMG_4442.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter numbers visible)
orientation: 0
confidence: high
notes: Visual Studio editor. Active tab "Eebrowser.asp"; tab "Main_ISLLSYS_20010101.vbs" still open alongside it. No line-number gutter visible. Closes out the "REPLACEMENT SEARCH PAGE" section (end section banner), then a short "Exception Handling" section (banner text appears twice - once inside the section like a heading comment, once after "End If" as a trailing label before the end-section marker; transcribed exactly as shown), then begins a new "Configure the frames and borders" section: sets mblnSearchPage=False by default, saves initial policy id via document.body.setAttribute "policyid", mstrPolicyID, has commented-out frameBorder assignment lines, then branches on CLng(mstrPolicyID): >0 shows menu/tree frames for policies (fraHorizontal.rows, fraVertical.cols [commented out with a leading straight-quote/typo], fraTreeColumn.rows); =0 sets mblnSearchPage=True (continues into next photo).
---
```vbs
        End If
    End If
                                                            REPLACEMENT SEARCH PAGE
    '
    '<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' >>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>>
    '
                                                            Exception Handling
    If Err.number <> 0 Then
        Err.clear
        Set mobjParentWindow = Nothing
    End If
                                                            Exception Handling
    '
    '<<<<<<<<<<<<<<<<<< [ end section ]<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

    ' >>>>>>>>>>>>>>>>>>[begin section]>>>>>>>>>>>>>>>>>>>>>>>
    '
                                                            Configure the frames and borders

        mblnSearchPage = False
        'Default

        ' Save the initial policy id.
        ' It will be restored after any ExectuteAction.
        ' This allows the policy id variable to be changed
        ' temporarily for navigation (as when the start page
        ' lanuches new policy windows)
        document.body.setAttribute  "policyid", mstrPolicyID


            'fraHorizontal.frameBorder = "0"
            'fraVertical.frameBorder = "0"
            'fraMain.frameBorder = "0"

    If CLng(mstrPolicyID) > 0 Then

            'show the menu and tree frames for policies
            fraHorizontal.rows = "46,*"
            'fraVertical.cols = "177,*"
            fraTreeColumn.rows = "90,*"

    ElseIf CLng(mstrPolicyID) = 0 Then

            mblnSearchPage = True
```
