---
photo: IMG_4625.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Same Visual Studio session; active tab (gold highlight) confirmed as
  Main_ISLLSYS_20010101.vbs. Continues the classid Select Case inside
  SetArrayData from IMG_4624: closes the ProtoView Combobox case (marrEEData(4)
  = ""), adds Case "CLSID:C2000000-FFFF-1100-8000-000000000004" ("Not used" /
  "Protoview Mask Control", reading objControl.TextWithMask into marrEEData(2))
  and Case "CLSID:C2000000-FFFF-1100-8200-000000000004" ("Not used" /
  "Protoview Numeric Control", reading GetControlValue(objControl) into
  marrEEData(2)), then End Select closes the classid dispatch. Back at the
  outer Select Case UCase(objControl.tagname) level: Case "ICHECKBOX" (maps
  GetControlValue to marrEEData(2), and marrEEData(3) to 1/0 for YES/NO),
  Case "RADIOBUTTON" (marrEEData(2)=GetControlValue, marrEEData(3)=
  objControl.selectedindex), and Case "XMLLIST" begins (marrEEData(2)=
  GetControlValue, then an If objControl.SelectedRowCount = 0 Then branch
  reading objControl.EmptyListNodeKey into marrEEData(3), cut off at bottom
  of frame -- continuation expected beyond this batch's range). Two "Not
  used" ProtoView CLSID cases (Mask and Numeric controls) suggest dead code
  paths kept for legacy/compatibility. This is the last photo in the
  requested IMG_4614-4625 range.
---
```vbscript
                    'objControl.ShowZero = "T"
                End If
                    'objControl.listindex < 0

                .marrEEData(4) = ""

            Case "CLSID:C2000000-FFFF-1100-8000-000000000004"
                'Not used
                '--Protoview Mask Control--
                .marrEEData(2) = objControl.TextWithMask
                .marrEEData(3) = ""
                .marrEEData(4) = ""

            Case "CLSID:C2000000-FFFF-1100-8200-000000000004"
                'Not used
                '--Protoview Numeric Control--
                .marrEEData(2) = GetControlValue(objControl)
                .marrEEData(3) = ""
                .marrEEData(4) = ""

        End Select
            'objControl.classid

        Case "ICHECKBOX"
            'need to pass text and index (X & L vars)
            .marrEEData(2) = GetControlValue(objControl)
            If .marrEEData(2) = "YES" Then
                .marrEEData(3) = 1
            Else
                'objControl.value = "NO"
                .marrEEData(3) = 0
            End If
            .marrEEData(4) = ""

        Case "RADIOBUTTON"
            .marrEEData(2) = GetControlValue(objControl)
            .marrEEData(3) = objControl.selectedindex
            .marrEEData(4) = ""

        Case "XMLLIST"
            .marrEEData(2) = GetControlValue(objControl)
            If objControl.SelectedRowCount = 0 Then
                .marrEEData(3) = objControl.EmptyListNodeKey
```
