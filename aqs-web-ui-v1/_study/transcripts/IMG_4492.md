---
photo: IMG_4492.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no line-number gutter shown in editor)
orientation: 0
confidence: high
notes: Same Visual Studio session; active tab still "Main_ISLLSYS_20010101.vbs" (Eebrowser.asp tab open but unfocused). Shows tail of mblnInquiryMode's End Function, then full Function mblnExposureActive() (checks SessionXML item "transactionid" against a hardcoded set of transaction-type codes 1,2,3,6,7,8,9,12,13 with inline comments naming each: Quote/Renewal/Amendements/Rewrites/Audits/Amendement Quote/Renewal Quote/Non-renewal/Rescind Non-renewal), then the start of Function mblnReadOnly (header comment only: "Returns True if Read only condition exists... transactionid = 4 or 5, or inquiry = T").
---

```vbs
    End Function
    '--------------------------------------------------------------------------------------

    Function mblnExposureActive()
    '****************************************************************************
    ' Returns  True if Edit or View of Exposures is allowed.
    '****************************************************************************
        Dim strValue
        On Error Resume Next
        strValue = ""
        mblnExposureActive = False

            '1-- Quote
            '2-- Renewal
            '3-- Amendements
            '6-- Rewrites
            '7-- Audits
            '8-- Amendement Quote
            '9-- Renewal Quote
            '12-- Non-renewal
            '13-- Rescind Non-renewal

        If SessionXML_GetItem ("transactionid",strValue) Then
            Select Case strValue
                Case "1", "2", "3", "6", "7", "8", "9", "12" ,"13"
                    mblnExposureActive = True
                Case Else
                    mblnExposureActive = False
            End Select
        End If

        Call TrapBrowserError("mblnExposureActive|1")
    End Function
    '--------------------------------------------------------------------------------------

    Function mblnReadOnly
    '****************************************************************************
    '   Returns  True if Read only condition exists, otherwise returns False.
    ' The current conditions for Read only mode include:
    ' transactionid = 4 or 5, or inquiry = T
    '****************************************************************************
        Dim strValue
        On Error Resume Next
```
