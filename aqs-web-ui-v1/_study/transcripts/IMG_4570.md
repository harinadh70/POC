---
photo: IMG_4570.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4569, same VS window/tabs (Main_ISLLSYS_20010101.vbs inactive, Eebrowser.asp active). Shows full
  Case "RADIOBUTTON","COMBO","KPCOMBO","IBUTTON","ICHECKBOX" block (VISIBLE/DISABLED handling via .enabled), then a new
  Case "XMLLIST","INPUT","SELECT","TEXTAREA","DIV","IMDBXMLLIST","TD" block beginning its own VISIBLE handling
  (style.visibility) and DISABLED handling -- this one tests `.disabled` directly rather than `.enabled`. No
  AQSMain/Main.vbs content visible. No line-number gutter.
---
```vbscript
        Case "RADIOBUTTON", "COMBO", "KPCOMBO", "IBUTTON", "ICHECKBOX"
        Select Case strAttribute
            Case "VISIBLE"
                Select Case UCase(.style.visibility)
                    Case "VISIBLE"
                        GetControlAttribute = "T"
                    Case "HIDDEN"
                        GetControlAttribute = "F"
                End Select
                    'UCase(.style.visibility)

            Case "DISABLED"
                If Not .enabled Then
                    GetControlAttribute = "T"
                Else
                    GetControlAttribute = "F"
                End If
                    'Not .enabled
        End Select
            'strAttribute

    Case "XMLLIST", "INPUT", "SELECT", "TEXTAREA", "DIV", "IMDBXMLLIST", "TD"

        Select Case strAttribute
            Case "VISIBLE"
                Select Case UCase(.style.visibility)
                    Case "VISIBLE"
                        GetControlAttribute = "T"
                    Case "HIDDEN"
                        GetControlAttribute = "F"
                End Select
                    'UCase(.style.visibility)

            Case "DISABLED"
                If .disabled Then
                    GetControlAttribute = "T"
                Else
                    GetControlAttribute = "F"
                End If
                    '.disabled
        End Select
            'strAttribute
```
