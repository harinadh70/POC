---
photo: IMG_4638.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; status bar cursor at Ln 2871, Ch 26)
orientation: 0
confidence: medium
notes: >
  Visual Studio 2022 editor, NOT the StarTeam/DevOps archive viewer used in prior batches.
  Two tabs open: "Main_ISLLSYS_20010101.vbs" (highlighted/active, tan background) and
  "Eebrowser.asp" (inactive, blue background) -- the ACTIVE tab is Main_ISLLSYS_20010101.vbs,
  a "Main_<LOB>_<date>.vbs" style top-level file distinct from Eebrowser.vbs/asp. This matches
  the "Main.vbs-like top-level frame-controller script" the task asked to watch for, though the
  visible code body is SetControlAttribute-style logic (Case "DISABLED"/"XMLLIST"/"IMDBXMLLIST"/
  "CLICKEDONCE", TrapBrowserError calls tagged "EEBrowser|SetControlAttribute|NN") -- i.e. it
  looks like a copy/variant of the same control-attribute-setting routine seen in Eebrowser.vbs,
  now living inside a Main_ISLLSYS_20010101.vbs file. No line-number gutter is visible in this
  editor view; solution/status bar shows caret at "Ln: 2871 Ch: 26", implying this file is far
  longer than the 1235-line Eebrowser.vbs already transcribed. Bottom-left of editor also shows
  a prior search box reading `Find "eebrowser.vbs"`, suggesting the user had just searched for
  eebrowser.vbs across the solution. Status bar branch indicator reads "develop", project
  "AQS.Advantage", startup project "AQS.Advantage.BatchEdit".
---
```vbnet
                .style.visibility = "visible"
                .style.display = ""
            Else
                .style.visibility = "hidden"
                If UCASE(.name) = "NEXT" Or UCASE(.name) = "BACK" Or  UCASE(.name) = "OK" or UCASE(.name) = "CANCEL" Then
                    .style.display = "none"
                End If
            End If
            Call TrapBrowserError("EEBrowser|SetControlAttribute|10")


        Case "DISABLED"
            .enabled = Not vntValue
            If .enabled Then
            '   If (UCase(.name) = "NEXT" And UCase(.getAttribute("text")) = "NEXT") Or _
            '       (UCase(.name) = "BACK" And UCase(.getAttribute("text")) = "NEXT") Or _
                IF (UCase(.name) = "NEXT" And UCase(.getAttribute("text")) = "BACK") Then
                    If Left(.getAttribute("image"),2) <> "Gr" Then
                        .image = "Gr" & .getAttribute("image")
                        .className = .className & "GR"
                    End If
                End If
            End If
            Call TrapBrowserError("EEBrowser|SetControlAttribute|11|" &  .name)
        Case "CLICKEDONCE"
            .ClickedOnce = Cbool(vntValue)

        End Select
            'strAttribute

    Case "XMLLIST", "IMDBXMLLIST"
        Select Case strAttribute
            Case "VISIBLE"
                If vntValue Then
                    .style.visibility = "visible"
                Else
                    .style.visibility = "hidden"
                End If
                Call TrapBrowserError("EEBrowser|SetControlAttribute|12")

            Case "DISABLED"
                .disabled = vntValue
                Call TrapBrowserError("EEBrowser|SetControlAttribute|13")
```
