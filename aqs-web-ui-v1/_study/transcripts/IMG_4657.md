---
photo: IMG_4657.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
lines: unknown (no gutter shown; editor status bar shows cursor at Ln 2871, Ch 26 -- unchanged from prior photos in this run)
orientation: 0
confidence: high
notes: >
  Same VS session/tabs as IMG_4650-4656. Finishes Sub BuildTab: applies the XSL
  transform (xmlProcessor.input/transform), writes result into
  .divTab(intIndex).innerHTML, then calls Call GetXMLData("[@tab='" & strMatchCode &
  "']", objCallingWindow) to load/set control values, cleans up xmlDoc/xmlTemplate/
  xmlProcessor, and ends with "End With" / "End Sub". Two new Functions begin:
  UnEscapeTextValueForXML(ByVal strToEscape) -- a single-line Replace() chain mapping
  custom placeholder tokens "#http39;"->"'", "#http60;"->"<", "#http62;"->">",
  "#http38;"->"&" (verbatim as displayed; these read like a non-standard/custom escape
  scheme rather than real HTML numeric entities, transcribed exactly as shown) -- and
  ShowZeroText(ByVal pobjControl), whose header comment explains it controls the "NONE"
  text shown when a combo needs to show NONE (used when the showzero attribute is "T").
---

```vbs
        Call TrapBrowserError("BuildTab|5")

        ' Load XML and transform
        xmlProcessor.input = xmlDoc
        xmlProcessor.transform
        Call TrapBrowserError("BuildTab|6")

        ' Load Tab DIV with transformed HTML
        .divTab(intIndex).innerHTML = xmlProcessor.output
        Call TrapBrowserError("BuildTab|7")

        If mblnDebug Then window.status = strDebugText & " - dynamic build"

        ' Load/Set the controls with the correct values and settings.
        Call GetXMLData("[@tab='" & strMatchCode & "']", objCallingWindow)
        Call TrapBrowserError("BuildTab|8")

        ' Clean up local objects
        Set xmlDoc = Nothing
        Set xmlTemplate = Nothing
        Set xmlProcessor = Nothing

            End If

        End If
        Call TrapBrowserError("BuildTab|9")

    End With
End Sub

Function UnEscapeTextValueForXML(ByVal strToEscape)
'****************************************************************************
'****************************************************************************
    UnEscapeTextValueForXML = Replace(Replace(Replace(Replace(strToEscape,"#http39;","'"),"#http60;","<"),"#http62;",">"),"#http38;","&")
End Function
'----------------------------------------------------------------------------------------

Function ShowZeroText(ByVal pobjControl)
'****************************************************************************
' This function provides the ability to control the "NONE" text when
' a combo needs to shown NONE.
' if the showzero attribute is T, "NONE" is used as the first item
' in the list.
```
