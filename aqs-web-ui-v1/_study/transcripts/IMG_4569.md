---
photo: IMG_4569.JPG
type: vscode-code
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no line-number gutter shown; status bar shows cursor at Ln 2871, Ch 26)
orientation: 0
confidence: high
notes: >
  Continuation of IMG_4568, same VS window/tabs (Main_ISLLSYS_20010101.vbs inactive, Eebrowser.asp active). Finishes the
  first CLSID Case block (VISIBLE/DISABLED handling), then a new Case for the AQS custom TreeView control referenced by
  its full ActiveX moniker string "../../SYSTEM/WINCONTROLS/AQSCONTROLLIBRARY1.DLL#AQSCONTROLLIBRARY.TREEVIEW" (commented
  "IE8 Tree Control"), repeating the same VISIBLE/DISABLED attribute-handling pattern. Then End Select / End Select
  closing the tagname Select Case, followed by a new Case "RADIOBUTTON","COMBO","KPCOMBO","IBUTTON","ICHECKBOX" beginning
  a nested Select Case strAttribute / Case "VISIBLE" (cut off at bottom of photo). No AQSMain/Main.vbs content visible.
  No line-number gutter.
---
```vbscript
                    Case "VISIBLE"
                        GetControlAttribute = "T"
                    Case "HIDDEN"
                        GetControlAttribute = "F"
                    End Select
                        'UCase(.style.visibility)

                Case "DISABLED"
                    If Not .enabled Then
                        GetControlAttribute = "T"
                    Else
                        GetControlAttribute = "F"
                    End If
                        'Not .enabled
            End Select
                'strAttribute
        Case "../../SYSTEM/WINCONTROLS/AQSCONTROLLIBRARY1.DLL#AQSCONTROLLIBRARY.TREEVIEW"
            '--*** IE8 Tree Control ***--
            Select Case strAttribute
            Case "VISIBLE"
                Select Case UCase(.style.visibility)
                Case "VISIBLE"
                    GetControlAttribute = "T"
                Case "HIDDEN"
                    GetControlAttribute = "F"
                End Select
                    'UCase(.style.visibility)

            Case "DISABLED"
                If Not .enabled Then
                    GetControlAttribute = "T"
                Else
                    GetControlAttribute = "F"
                End If
                    'Not .enabled
            End Select

    End Select
        'UCase(.tagname)

    Case "RADIOBUTTON", "COMBO", "KPCOMBO", "IBUTTON", "ICHECKBOX"
        Select Case strAttribute
            Case "VISIBLE"
```
