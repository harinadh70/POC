---
photo: IMG_4438.JPG
type: vbs-archive
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - originally mislabeled Eebrowser.asp; direct raw-photo tab-bar inspection confirms Main_ISLLSYS_20010101.vbs (tan/gold highlighted tab) was the active tab, Eebrowser.asp was the inactive background tab throughout this run of photos."
lines: unknown (no gutter shown)
orientation: 0
confidence: medium
notes: >
  Same Visual Studio session/tabs as prior photos in this batch. Continues the
  "mstrNextAction_*" parameter-mapping comments started in IMG_4437 (mstrNextAction_Action
  = pstrAction). Verified by zoom that the Dim column and its "= pXxx" comment column are
  visually offset by one row here (each comment trails one row below/right of its true
  Dim, same drift pattern seen in IMG_4435/4436) — confirmed against IMG_4437's
  unambiguous mstrNextAction_Action/pstrAction pairing, so pairing below is corrected via
  that established offset. By contrast, further down in this SAME photo (mxmlStyle /
  mxmlProcessor / mblnPolicyDiscard block), a follow-up zoom showed TRUE same-row
  alignment (mxmlProcessor genuinely pairs with the xsl-file comment, mblnPolicyDiscard
  genuinely pairs with the Policy-Discard comment) — i.e. the row-drift is not a constant
  global offset but varies locally with blank-line spacing, so each block was individually
  verified rather than assuming one rule for the whole file. mxmlStyle has no comment
  shown. After the declarations, this photo shows the first actual executable
  initialization code seen in this batch (an "' Initialize variables" block assigning
  default values to many of the m-prefixed module variables, plus Set ... = Nothing
  calls) — not just Dim statements. Still no line-number gutter visible.
---
```vbscript
Dim mstrNextAction_NodeKey                                     ' = pstrNodeKey

Dim mstrNextAction_Tab                                         ' = pintTab

Dim mstrNextAction_XMLDetail                                   ' = pstrXMLDetail

' Used to indicate to the browser command NAVIGATE_CYCLING that
' the call to ExecuteAction should be deferred until the modal attempting
' the navigation has time to close.  This variable is managed in ExecuteAction
' and checked in ExecuteBrowserCommands (NAVIGATE_CYCLING case)
Dim mblnNextAction_DeferNavigation

Dim mxmlStyle

Dim mxmlProcessor                      'Holds the xsl file contents for transforming xml to html

Dim mblnPolicyDiscard                  'Holds True or False if Policy Discard is selected from Menu

' Initialize variables
  mstrPolicyNumber = vbNullString
  mstrPrimaryInsured = vbNullString
  mstrPolicyID = "0"
  mstrNodeKey = "POL|POL|0"
  mstrExpiredNumber = "0"
  Set mdicChildWindows = Nothing
  Set mdicNonPolicyChildWindows = Nothing
  Set mdicExternalWindows = Nothing
  Set mxmlBatchEdits = Nothing
  Set mxmlStyle = Nothing
  mlngConversionReportID = 0
  Set mxmlSecurity = Nothing
  mstrDiagnosticMode = "0"
  mstrUserID = "NOTASSIGNED"
  mstrXMLDetail = "<items />"
  mstrTransactionType = ""

  Set mvntDebugWindow = Nothing
  mblnDebug = False
  mblnTreeMessages = False
  mblnDeveloperMessages = False
```
