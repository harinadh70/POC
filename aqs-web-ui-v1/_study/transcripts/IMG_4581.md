---
photo: IMG_4581.JPG
type: other
file: Main_ISLLSYS_20010101.vbs
correction: "2026-07-14 - re-verified via direct tab-bar inspection; originally mislabeled Eebrowser.asp, true active tab is Main_ISLLSYS_20010101.vbs."
lines: unknown (no gutter line numbers visible; editor cursor status bar shows "Ln: 2871, Ch: 26")
orientation: 0
confidence: high
notes: >
  IMPORTANT: The background/pinned tab name is now FULLY visible (not cut off as in
  IMG_4578-4580): "Main_ISLLSYS_20010101.vbs". This is a strong candidate match for the
  "Main.vbs / AQSMain.vbs" top-level frame-controller script the batch is specifically
  looking for -- however this photo's editor CONTENT still belongs to the active
  "Eebrowser.asp" tab (code is a direct continuation of IMG_4580: finishes the SELECT-tag
  branch with Call FillSelectList(...), then opens a new "Look for an COMBO tag" section
  for tagname "COMBO" or "KPCOMBO" implementing AQS Combo Box Behavior, LimitToList,
  ShowZero, and CutPaste attribute wiring). The Main_ISLLSYS_20010101.vbs tab's own
  contents were NOT visible/captured in this photo -- only its tab label. Same Visual
  Studio IDE / AQS.Advantage solution as prior photos in this batch.
---

```vbs
                strRelatedList = objNamedNodeMap.getNamedItem("excludelist").nodeValue
            End If
            Call FillSelectList(objControl, objNode, strRelatedList, objCallingWindow)
        End If
            'UCase(objControl.tagname) = "SELECT"
        '///////////////////////////////////////////////////////////////////////

        '///////////////////////////////////////////////////////////////////////
        '--Look for an COMBO tag--
        If UCase(objControl.tagname) = "COMBO" or UCase(objControl.tagname) = "KPCOMBO" Then
            '--AQS Combo Box Behavior--
            'Test to see if the combobox is limited to the list and set the routine-level
            'flag. This will be used later to determine if the X(ListIndex) or L(Text)
            'variable should be used when setting the XMLDI data value of the combobox.

            'Defer loading the items until the user selects the control or the value
            'is set by the listindex(X)
            If objControl.LimitToList = "F" Then
                blnLimitToList = False
            End If

            If Not objNamedNodeMap.getNamedItem("ShowZero") Is Nothing Then
                ' Update the control with the Show Zero value from the xml
                objControl.setAttribute "ShowZero", objNamedNodeMap.getNamedItem("ShowZero").nodeValue
            End If

            If blnCutPaste Then
                If Not objNamedNodeMap.getNamedItem("CutPaste") Is Nothing Then
                    If StrComp(objNamedNodeMap.getNamedItem("CutPaste").nodeValue, "T", vbTextCompare) = 0 Then
                        objControl.CutPaste = True
                    Else
                        objControl.CutPaste = False
                    End If
                Else
                    objControl.CutPaste = False
                End If
            Else
                objControl.CutPaste = False
            End If

        End If
            'UCase(objControl.tagname) = "COMBO" or UCase(objControl.tagname) = "KPCOMBO"
        '///////////////////////////////////////////////////////////////////////
```
